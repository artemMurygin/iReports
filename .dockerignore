# Используется и backend, и frontend (оба собираются из корня репозитория, docker-compose.yml:
# backend.build.context: ., frontend.build.context: .) — оба зависят от workspace-пакета
# ireports-contracts, который резолвится только относительно корня монорепо.

**/node_modules
**/dist
**/*.log
.env
**/.env
.git
.idea
.vscode
.github

# Старая версия backend, не участвует в сборке
backend/deprecated
