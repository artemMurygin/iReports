# Plan: Закрытие месяца, начисление на баланс и выплата зарплаты

**PRD**: [`README.md`](./README.md) → [PRD 1](./prd-accounting-period-closing-pipeline.md) · [PRD 2](./prd-salary-accrual-and-employee-balance.md) · [PRD 3](./prd-salary-payout-and-erp-cash-documents.md)
**Дата**: 2026-08-20

## Контекст из кода

- Закрытие/переоткрытие/пересчёт периода уже есть: `CloseAccountingPeriodHandler` (`service`), `CloseShopAccountingPeriodHandler` (`shop`), generic `get`/`reopen`/`recalculate`; модели `AccountingPeriod`, `AccountingPeriodSnapshot`, `AccountingCalculationCache` (`backend/prisma/schema/accounting-period.prisma`). Снапшот хранит `lines: Json` (`RuleBreakdownLine[]`).
- Источник часов — `EmployeeHoursEntry` (`/v1/{direction}/accounting/employee_hours*`). График работы реализован в соседней сессии и влит в `feat/salary` (merge `31d63d3`); read-only состояние закрытого месяца в UI графика — его зона, здесь только гард на бэкенде. Страница отчёта `/salaries` уже существует (`9a71a1e`).
- Синхронизация ERP — кроны `@ProdCron` (`RoappSyncCron`, `moysklad-sync`), ручного запуска нет. Записи в ERP в проекте нет вообще — только чтение.
- Фронт: `pages/SalesPlan` (без статуса периода — комментарий в `ui/PageHeader.tsx`), `pages/SalaryRules`; страниц зарплатного отчёта, начислений, баланса нет. Мобильный шелл: App Bar (`kXibe`) и Bottom Nav (`XXiyY`) из `design/sallary-first-iteration.pen` уже в `shared/ui-kit`.
- Дизайн ведётся в `design/sallary-first-iteration.pen` (Pencil); новые экраны добавляются туда же, в стиле существующих («План продаж · Список», «План продаж · Мобильный», экраны зарплатных схем).

## Порядок фаз

Фазы сгруппированы по PRD и идут строго последовательно: PRD 1 (фазы 1–5) → PRD 2 (фазы 6–10) → PRD 3 (фазы 11–15). Дизайн каждой группы страниц — отдельная фаза **до** соответствующих фронтенд-фаз, чтобы вёрстка шла по макету, как у плана продаж. После любой фазы можно остановиться с рабочим результатом.

```
PRD 1:  1 BE tracer → 2 BE синк+блокировка часов → 3 Design → 4 FE закрытие → 5 FE начисления
PRD 2:  6 BE проведение → 7 BE баланс → 8 Design → 9 FE документ → 10 FE баланс
PRD 3: 11 BE адаптеры ERP → 12 BE выплата → 13 Design → 14 FE выплата → 15 FE удаление/ERP в формах
```

---

## PRD 1 — Закрытие месяца и документы начисления

### Фаза 1: Документы начисления при закрытии (tracer bullet)
**Цель**: Закрытие периода по API порождает документы начисления `DRAFT` по каждому сотруднику снапшота — сквозной путь «закрыть → документы в БД → прочитать списком» работает для `service` и `shop`.
**Что затрагивает?** backend, database, contracts
**Задачи:**
- [x] `contracts`: `salaryAccrualStatusSchema` (`DRAFT | PARTIALLY_ACCRUED | ACCRUED | PAID`), `salaryAccrualLineStatusSchema`, `salaryAccrualLineSchema`, `salaryAccrualSchema` (с `isDismissed`), ответы списка и карточки
- [x] Prisma: `SalaryAccrual` (уникальность `(direction, period, employeeId)`, `isDismissed`, `total`) и `SalaryAccrualLine` (нормализованные поля `RuleBreakdownLine` + `originalAmount`/`amount`); миграция
- [x] В `CloseAccountingPeriodHandler` и `CloseShopAccountingPeriodHandler` (независимо, без общего кода): сброс кэша периода перед расчётом, создание документов в той же транзакции `UnitOfWork`, что снапшот и `CLOSED`; `isDismissed` — из активности `BitrixEmployee`; событие `SalaryAccrualDocumentsCreated`
- [x] `GET /v1/{direction}/accounting/salary_accruals?period`, `GET .../salary_accruals/:id`; ответ зарплатного отчёта за закрытый период дополняется статусом документа
- [x] `reopen`: через порт проверяет документы периода — все `DRAFT` → удаляет документы и снапшот, иначе `409` с перечнем
**Тесты**: unit на хендлеры закрытия (документ на каждого сотрудника снапшота, включая нулевых и уволенных; сумма = `total` снапшота, строки = `lines`; кэш сброшен — тест с изменением данных после прогрева; инъекция ошибки → период открыт, ничего не создано); e2e `close → list → reopen`, `reopen` с документом не в `DRAFT` → `409`; закрытие `service` не трогает `shop`.
**Когда готово**: `POST .../period/:period/close` создаёт документы, `GET .../salary_accruals?period` их отдаёт, `reopen` их удаляет; все перечисленные тесты зелёные; `ENDPOINTS.md` обновлён.

### Фаза 2: Неявная синхронизация ERP, сводка закрытия, блокировка часов
**Цель**: Закрытие само дотягивает данные из ERP за месяц и отклоняется при ошибке синка; есть `close-preview` для окна подтверждения; часы закрытого месяца заблокированы.
**Что затрагивает?** backend, contracts
**Задачи:**
- [x] Вызов синка по требованию из хендлеров закрытия: `service` — заказы RemOnline за месяц, `shop` — отгрузки МойСклада за месяц; таймаут (2 мин), блокировка по направлению от параллельного тика крона; ошибка синка → закрытие отклоняется, период открыт
- [x] Ограничение «только истёкший месяц» в хендлерах закрытия (текущий/будущий → `409`)
- [x] `GET /v1/{direction}/accounting/period/:period/close-preview` → `{ employeesCount, dismissedEmployeesCount, totalAmount, unapprovedPlanRows, employeesWithoutHours }` (контракт `closePeriodPreviewSchema`)
- [x] Единый переиспользуемый гард/доменный сервис «период закрыт» по `AccountingPeriod`; подключить к `POST/PATCH/DELETE employee_hours*` → `409` с `closedBy`/`closedAt`
**Тесты**: синк вызван до расчёта и новый заказ попадает в снапшот (замоканная ERP); ошибка/таймаут синка → `409`, ничего не создано; параллельный запуск синка и крона по направлению — один выполняется; `close-preview` совпадает с результатом реального закрытия; часы закрытого месяца → `409`, другого месяца/направления — ок; после `reopen` часы снова редактируются.
**Когда готово**: закрытие в dev-среде (где крон не тикает) даёт актуальные данные; `employee_hours` закрытого месяца не редактируются; тесты зелёные.

### Фаза 3: Дизайн — закрытие месяца и документы начисления
**Цель**: Макеты в `design/sallary-first-iteration.pen` для всех экранов PRD 1 (десктоп ≥1440 + мобильный 390), согласованные с существующими экранами плана продаж.
**Что затрагивает?** design
**Задачи:**
- [x] Экран «План продаж · статус периода + закрытие месяца» — блок статуса и кнопка в `PageHeader`, окно подтверждения, состояния ошибок
- [x] Экран «Начисления · Список за месяц»
- [x] Экран «Начисление · Документ сотрудника»
- [x] Экспорт Node ID каждого экрана в этот план (таблица «Экран → Node ID») для фаз 4–5
**Промты для Pencil** (каждый — отдельный запрос к `pencil`, файл `design/sallary-first-iteration.pen`; перед началом — `get_app_state` со схемой и `get_guidelines`):

> **P1.1 — Закрытие месяца на странице плана продаж.** Открой `design/sallary-first-iteration.pen`, возьми за основу экран «План продаж · Список» (`x2soj`) и его мобильную версию (`hYl0B`). Добавь в Page Header блок статуса расчётного периода: индикатор «Период открыт» / «Период закрыт · Иван Петров · 01.08.2026 14:20» и primary-кнопку «Закрыть месяц» справа от Period Chip; для закрытого периода вместо неё — secondary-кнопка «Переоткрыть месяц». Для текущего/будущего месяца кнопка disabled с tooltip «Месяц ещё не закончился». Нарисуй модальное окно подтверждения «Закрыть июль 2026 · Сервис» с контрольными цифрами в виде 4 KPI-карточек (переиспользуй `KpiCard`): «Сотрудников к начислению» (с подписью «из них уволены: 2»), «Фонд оплаты», «Без часов (PayPerHour)», «Неутверждённых строк плана»; под ними — список неутверждённых строк плана со ссылкой «Утвердить» у каждой и предупреждение, блокирующее primary-кнопку «Закрыть месяц», пока список не пуст. Нарисуй три состояния окна: готово к закрытию, заблокировано планом, ошибка «Не удалось получить данные из ERP, повторите позже» (inline alert, кнопка «Повторить»). Индикатор выполнения закрытия — обычный спиннер на кнопке, без отдельных шагов. Сделай десктоп (1440) и мобильную (390, bottom-sheet вместо модалки) версии, используя App Bar `kXibe` и Bottom Nav `XXiyY`.

> **P1.2 — Список начислений за месяц.** В том же файле создай экран «Начисления · Список» в стиле `x2soj`: Page Header «Начисления зарплаты» + Direction Tabs (Сервис/Магазин) + Period Chip; KPI Row из 4 карточек: «Фонд оплаты», «Сотрудников», «Черновик», «Ожидает выплаты». Таблица: чекбокс, Сотрудник (аватар-инициалы + ФИО + отдел, бейдж «Уволен» при необходимости), Правил (число), Сумма ₽, Статус (бейджи: Черновик — серый, Частично начислено — жёлтый, Ожидает выплаты — синий, Выплачено — зелёный), Прогресс («3 из 5» + тонкая полоса), Действия (иконка «открыть»). Над таблицей Selection Bar с кнопкой «Начислить выбранным» и «Начислить все документы месяца» (с confirm-модалкой «Будет начислено N документов на сумму X ₽»). Фильтр-чипы по статусу. Empty-state «Месяц ещё не закрыт — закройте его на странице плана продаж» со ссылкой. Мобильная версия — список карточек по образцу `i4Qz8y` (`Plan Card`): ФИО, сумма, статус-бейдж, прогресс.

> **P1.3 — Документ начисления сотрудника.** Создай экран «Начисление · Документ»: шапка — ФИО, отдел, направление, период, статус-бейдж, итоговая сумма крупно, кнопки «Начислить всё» (primary) и «Назад к списку». Ниже — компактный блок план/факт отдела (переиспользуй `KpiCard`×3: План, Факт, Выполнение %). Таблица строк по правилам: Правило (название + тип мелким), Роль (чип: Инженер / Онлайн-менеджер / …), База, Кол-во, Ставка, Сумма ₽ (если есть корректировка — исходная сумма зачёркнута, рядом новая и иконка комментария), Статус строки (Черновик / Начислено), Действия: «Начислить» (для Черновика), «Отменить начисление» (для Начислено, ghost-кнопка, красный текст), «Корректировать» (иконка карандаша, только Черновик). Строка раскрывается аккордеоном в список источников (номер заказа / продажа / часы / задача, дата, сумма). Нарисуй drawer «Корректировка строки»: исходная сумма (read-only), новая сумма, обязательный комментарий, кнопки «Сохранить» / «Отмена». Состояния: документ Черновик, Частично начислен, Ожидает выплаты (все строки начислены, кнопка «Начислить всё» скрыта), Выплачено (все действия скрыты, бейдж зелёный). Мобильная версия — карточки строк.


**Когда готово**: все экраны и состояния есть в `.pen`, Node ID записаны в таблицу ниже, скриншоты через `mcp__pencil__get_screenshot` согласованы с пользователем.

Все макеты фазы собраны в секции канвы **«Закрытие месяца и начисления»** (`uKNkE`, ~`x: 1657, y: 17880`).

| Экран | Node ID (десктоп) | Node ID (мобильный) |
|---|---|---|
| План продаж · закрытие месяца | `ePDd7` (период открыт), `a1Mmrh` (период закрыт), `lu0Bl` (3 состояния блока статуса: открыт / закрыт / текущий месяц с disabled-кнопкой и tooltip) | `GfwNx` |
| Модалка «Закрыть месяц» | `GUo20` (готово), `KPPJ5` (заблокировано планом), `hhWeF` (ошибка ERP), `xL1JD` (выполняется, спиннер) | `o6vgAh` (готово), `sAd8Z` (заблокировано планом), `L1OLgC` (ошибка ERP) — bottom-sheet |
| Начисления · Список | `cfNlL`, `g6vEv` (empty-state «месяц ещё не закрыт»), `yDI1H` (confirm «Начислить все документы месяца») | `Q0i6z3` |
| Начисление · Документ | `jb7fL` (Черновик), `JEdfH` (Частично начислен), `fX0wq` (Ожидает выплаты), `L6cTJ` (Выплачено), `w1BVLM` (drawer «Корректировка строки») | `wYi5o` |

### Фаза 4: Фронтенд — закрытие месяца на странице плана продаж
**Цель**: Руководитель закрывает и переоткрывает месяц из UI с окном подтверждения по макету P1.1.
**Что затрагивает?** frontend
**Задачи:**
- [x] `features/AccountingPeriod/model/api.ts`: `queryOptions` для `GET period/:period`, `GET close-preview`, мутации `close`/`reopen` (по направлению); инвалидация запросов плана продаж и отчётов
- [x] Блок статуса периода и кнопки «Закрыть месяц» / «Переоткрыть месяц» в `pages/SalesPlan/ui/PageHeader.tsx`; disabled для текущего/будущего месяца; признак закрытого месяца в `PeriodPicker`
- [x] `features/AccountingPeriod/ui/ClosePeriodDialog` — KPI-сводка из `close-preview`, перечень неутверждённых строк со ссылкой «Утвердить», блокировка кнопки, состояния ошибки синка (`409`/`503`) с «Повторить»; мобильный bottom-sheet
- [x] Диалог подтверждения переоткрытия; обработка `409` с перечнем документов не в `DRAFT`
- [x] После успешного закрытия — переход на `/salary-accruals?period&direction`
**Тесты**: компонентные тесты диалога (три состояния), e2e в браузере на периоде с утверждённым планом: закрыть → статус «Закрыт» → переоткрыть.
**Когда готово**: скриншоты совпадают с макетами P1.1 (десктоп/мобильный); закрытие из UI создаёт документы; `npm run build`/`lint` без ошибок.

### Фаза 5: Фронтенд — список начислений и карточка документа (чтение)
**Цель**: Страницы списка и документа начисления по макетам P1.2/P1.3 в режиме чтения (действия появятся в фазе 9).
**Что затрагивает?** frontend
**Задачи:**
- [x] Маршруты `/salary-accruals` и `/salary-accruals/:id`, пункт меню «Начисления» в разделе «Зарплата» (`app/Header.tsx`, Bottom Nav)
- [x] `features/SalaryAccruals/model/api.ts` — `queryOptions` для списка и карточки, типы из `ireports-contracts`
- [x] `pages/SalaryAccruals` — Page Header, Direction Tabs, Period Chip, KPI Row, таблица со статусами/прогрессом/бейджем «Уволен», фильтр по статусу, empty-state «месяц не закрыт»; мобильные карточки
- [x] `pages/SalaryAccrualDocument` — шапка, блок план/факт, таблица строк с аккордеоном источников, бейджи статусов; мобильные карточки
- [x] Статус документа в отчёте сотрудника (если страница отчёта есть к этому моменту — бейдж; иначе только типы)
**Тесты**: компонентные тесты таблицы (статусы, прогресс, корректировка с зачёркнутой суммой), проверка в браузере на закрытом периоде.
**Когда готово**: скриншоты совпадают с P1.2/P1.3; реальные документы за закрытый месяц отображаются; `build`/`lint` чистые.

---

## PRD 2 — Начисление на баланс и баланс сотрудника

### Фаза 6: Проведение строк и лента баланса (tracer bullet PRD 2)
**Цель**: «Начислить» строку создаёт движение на балансе; остаток читается как сумма ленты; отмена удаляет движение.
**Что затрагивает?** backend, database, contracts
**Задачи:**
- [x] `contracts`: `balanceTransactionTypeSchema` (полный перечень, включая `ACCRUAL_ADJUSTMENT`, `MANUAL_REVERSAL`, `PAYOUT`), `balanceTransactionSchema`, ответ баланса сотрудника, запросы accrue/unaccrue/корректировки
- [x] Prisma: `BalanceTransaction` (`employeeId`, `direction`, `type`, `amount` со знаком, `occurredAt`, `createdBy`, `comment`, `period?`, `accrualId?`, `lineId?`, `ruleId?`, `reversedTransactionId?`, `erpSyncRequired`); уникальный индекс `(lineId, type)` для идемпотентности; `SalaryAccrualLine.status`, история корректировок
- [x] Команды `AccrueLine` (движение `SALARY_ACCRUAL` на сумму снапшота + `ACCRUAL_ADJUSTMENT` на разницу при корректировке, одна транзакция), `UnaccrueLine` (удаление обоих движений + `DRAFT`, `409` для `PAID`), `AdjustLine` (только `DRAFT`, обязательный комментарий); пересчёт статуса документа из строк
- [x] `GET /v1/{direction}/accounting/balance/employee/:id?from&to&types` — остаток = `SUM`, лента с фильтрами и итогом по выборке, раскрытие начисления до строки документа
- [x] `reopen` периода: проверка документов не в `DRAFT` (уже из фазы 1) — сквозной тест с проведённой строкой
**Тесты**: unit на команды (ровно одно движение; параллельный двойной accrue → одно движение по индексу; корректировка → два движения, сумма = новая; unaccrue удаляет оба; корректировка в `ACCRUED` → `409`; без комментария → `400`); остаток = сумма смешанной ленты; e2e `close → accrue → balance → unaccrue → reopen`.
**Когда готово**: через API можно провести строку, увидеть остаток, отменить; тесты зелёные; `ENDPOINTS.md` обновлён.

### Фаза 7: Массовое проведение, ручные движения, сторно, сводка по отделу
**Цель**: Полный набор операций PRD 2 на бэкенде.
**Что затрагивает?** backend, contracts
**Задачи:**
- [x] `POST .../salary_accruals/:id/accrue` (документ) и `POST .../salary_accruals/accrue?period` (месяц) — построчно в своих транзакциях, ответ с перечнем неудачных
- [x] `POST .../balance/employee/:id/transactions` — ручные движения `ADVANCE`, `EXTRA_ADVANCE`, `BONUS`, `SICK_LEAVE`, `VACATION_PAY`, `PENALTY`, `ADJUSTMENT`; знак по типу, для `ADJUSTMENT` явно; обязательный комментарий для `PENALTY`/`ADJUSTMENT`; дата задним числом разрешена; `erpSyncRequired` только хранится
- [x] `POST .../balance/transactions/:id/reverse` — `MANUAL_REVERSAL` только для ручных движений без документа ERP, обязательный комментарий, пометка исходного «сторнировано»; `PATCH`/`DELETE` движения не существует
- [x] `GET .../balance/department/:id/:period` — остаток / начислено / авансы / ручные по сотрудникам текущего отдела (из Bitrix24), итог по отделу
**Тесты**: массовое проведение с инъекцией ошибки на одной строке → остальные проведены, ошибка в ответе; каждый тип ручного движения; `PENALTY` без комментария → `400`; сторно ручного и запрет сторно начисления/движения с ERP; движение в закрытом месяце не меняет снапшот; итоги отдела = сумма сотрудников.
**Когда готово**: все эндпоинты PRD 2 работают; тесты зелёные.

### Фаза 8: Дизайн — проведение начислений, баланс сотрудника, сводка отдела
**Цель**: Макеты интерактивных состояний документа начисления, страницы баланса и сводки по отделу.
**Что затрагивает?** design
**Задачи:**
- [ ] Доработка экрана «Начисление · Документ» (P1.3) действиями и drawer корректировки — уже в P1.3; проверить и дополнить состояниями массовых операций (результат с ошибками)
- [ ] Экран «Баланс сотрудника» (руководитель и сотрудник)
- [ ] Drawer «Добавить приход / расход»
- [ ] Экран «Балансы отдела»
- [ ] Записать Node ID в таблицу
**Промты для Pencil**:

> **P2.1 — Результат массового начисления.** К экрану «Начисления · Список» добавь модалку результата после «Начислить все документы месяца»: сводка «Начислено 42 из 45 документов на 1 234 567 ₽», список из 3 ошибок (ФИО, правило, текст ошибки) с кнопкой «Повторить для неудачных». Десктоп и мобильный.

> **P2.2 — Баланс сотрудника.** Создай экран «Баланс · Сотрудник» в стиле `x2soj`. Шапка: аватар-инициалы, ФИО, отдел, Direction Tabs (Сервис / Магазин / Все). KPI Row: «Остаток · Сервис», «Остаток · Магазин», «Итого» (отрицательный остаток — красным со знаком минус, без предупреждений), «Начислено за месяц». Справа в шапке две кнопки руководителя: «Добавить приход» (primary, иконка +) и «Добавить расход» (secondary, иконка −). Фильтры: Period Chip (месяц), мультиселект типов движений (чипы: Начисление, Корректировка, Аванс, Доп. аванс, Премия, Больничный, Отпускные, Штраф, Корректировка вручную, Выплата). Лента — таблица: Дата (с подписью «создано 05.08» мелким, если создано позже даты движения), Тип (иконка + название, цвет: приход зелёный / расход красный), Сумма ₽ со знаком, Комментарий, Период, Автор, ERP (иконка-ссылка с внешним ID, если есть документ ERP; пустая, если нет), Действия (для ручных без ERP — «Сторно»; для движений с ERP — «Удалить»; для начислений — пусто). Строка типа «Начисление» раскрывается до правила и источников (как в документе). Сторнированная строка — серая, зачёркнутая сумма, бейдж «Сторнировано». Внизу — итог по выборке. Нарисуй два варианта: для руководителя (с кнопками и действиями) и для сотрудника в личном кабинете (только чтение, без кнопок, Direction Tabs сохранены). Мобильная версия — карточки движений с группировкой по дням.

> **P2.3 — Добавить приход / расход.** Drawer (десктоп) / bottom-sheet (мобильный) «Новое движение»: сегмент-контрол Приход / Расход; выбор типа (radio-карточки с иконками: для прихода — Премия, Больничный, Отпускные, Корректировка; для расхода — Аванс, Доп. аванс, Штраф, Корректировка); направление (Сервис / Магазин); сумма (крупное поле); дата (по умолчанию сегодня, можно задним числом); комментарий (обязателен для Штраф и Корректировка — помечено звёздочкой, для остальных — опционален); переключатель «Провести в кассе ERP» с подсказкой «Создаст расходный ордер в МойСкладе / движение по кассе RemOnline»; предпросмотр «Остаток после: 12 300 ₽ → 7 300 ₽». Кнопки «Создать» / «Отмена». Состояния: валидация комментария, ошибка ERP inline («Не удалось создать документ в ERP: …», кнопка «Повторить»). Отдельно — маленькая confirm-модалка «Сторно движения» с обязательным комментарием.

> **P2.4 — Балансы отдела.** Экран «Баланс · Отдел»: Page Header «Балансы сотрудников» + селект отдела + Direction Tabs + Period Chip; KPI Row: «Остаток по отделу», «Начислено за месяц», «Выдано авансов», «Ручные движения». Таблица: Сотрудник (инициалы + ФИО), Остаток ₽ (минус — красным), Начислено, Авансы, Ручные (+/−), Статус начисления (бейдж документа), Действие «Открыть баланс». Строка итога внизу. Мобильная версия — карточки.

**Когда готово**: экраны в `.pen`, Node ID в таблице, скриншоты согласованы.

| Экран | Node ID (десктоп) | Node ID (мобильный) |
|---|---|---|
| Начисления · результат массовой операции | — | — |
| Баланс · Сотрудник (руководитель) | — | — |
| Баланс · Сотрудник (личный кабинет) | — | — |
| Drawer · Новое движение | — | — |
| Баланс · Отдел | — | — |

### Фаза 9: Фронтенд — действия в документе и списке начислений
**Цель**: Руководитель проводит, корректирует и отменяет строки; массовые операции по документу и месяцу.
**Что затрагивает?** frontend
**Задачи:**
- [ ] Мутации `accrueLine` / `unaccrueLine` / `adjustLine` / `accrueDocument` / `accruePeriod` в `features/SalaryAccruals/model`, инвалидация карточки, списка и баланса
- [ ] Действия строки в `pages/SalaryAccrualDocument`: «Начислить», «Отменить начисление» (confirm), «Корректировать» (drawer с обязательным комментарием); кнопка «Начислить всё»; скрытие действий для `PAID`
- [ ] Selection Bar и «Начислить все документы месяца» в `pages/SalaryAccruals` с confirm и модалкой результата (P2.1) с «Повторить для неудачных»
- [ ] Оптимистичное обновление прогресса «N из M» и статуса документа
**Тесты**: компонентные тесты действий и состояний; e2e в браузере: провести строку → отменить → скорректировать → провести → «Начислить всё» → статус «Ожидает выплаты».
**Когда готово**: скриншоты совпадают с P1.3/P2.1; сквозной сценарий проходит; `build`/`lint` чистые.

### Фаза 10: Фронтенд — баланс сотрудника, новое движение, сводка отдела
**Цель**: Страницы баланса по макетам P2.2–P2.4.
**Что затрагивает?** frontend
**Задачи:**
- [ ] Маршруты `/balance/employee/:id`, `/balance/department`, пункты меню «Балансы» в разделе «Зарплата»; `features/EmployeeBalance/model/api.ts` (баланс с фильтрами, сводка отдела, мутации создания/сторно)
- [ ] `pages/EmployeeBalance` — KPI остатков по направлениям и итог (минус красным), фильтры периода/типов, лента с раскрытием начислений, бейдж «Сторнировано», итог по выборке; режим «только чтение» для личного кабинета (по флагу маршрута до появления прав)
- [ ] `features/EmployeeBalance/ui/NewTransactionDrawer` — приход/расход, типы, направление, сумма, дата, комментарий с валидацией, переключатель `erpSyncRequired` (в этой фазе только сохраняется), предпросмотр остатка
- [ ] Confirm «Сторно» с комментарием; скрытие сторно для начислений и движений с ERP
- [ ] `pages/DepartmentBalances` — селект отдела, KPI, таблица с итогом, переход в баланс сотрудника; мобильные карточки
**Тесты**: компонентные тесты ленты (знаки, сторно, раскрытие), drawer (валидация комментария); e2e: создать аванс → остаток уменьшился → сторно → остаток вернулся.
**Когда готово**: скриншоты совпадают с P2.2–P2.4; сценарий проходит на реальных данных; `build`/`lint` чистые.

---

## PRD 3 — Выплата и кассовые документы ERP

### Фаза 11: Адаптеры записи в ERP и конфигурация кассы
**Цель**: iReports умеет создавать и удалять кассовый документ в RemOnline (движение по кассе) и МойСкладе (расходный/приходный ордер со статьёй) — без привязки к выплате.
**Что затрагивает?** backend, database, contracts
**Задачи:**
- [ ] Подтвердить по API (через `RoApp MCP` и `moysklad MCP`): RemOnline — создание/удаление движения по кассе; МойСклад — `cashout`/`cashin` с `expenseItem`, организацией и агентом-сотрудником, `DELETE`. Если удаление не поддерживается — **стоп, блокер**, вернуться к пользователю
- [ ] `contracts`: `erpCashDocumentSchema`, `erpCashConfigSchema` (`service`: `roappCashboxId`; `shop`: `moySkladExpenseItemId`, `moySkladIncomeItemId`, `organizationId`)
- [ ] Prisma: `ErpCashDocument` (`transactionId` unique, `system`, `kind`, `amount`, `externalId`, `createdAt`), `ErpCashConfig` по направлению; `GET|PUT /v1/{direction}/accounting/erp_cash_config`
- [ ] Порты `ErpCashDocumentPort { create, delete, findByKey }` и адаптеры в `domains/service/integrations/roapp` и `domains/shop/integrations/moySklad`: конвертация `Int` → рубли с копейками, назначение «Зарплата за 2026-07 / Аванс / Премия», агент через `EmployeeIdentity`, таймаут 15 с
**Тесты**: контрактные тесты адаптеров на форму запроса (без реальных вызовов); `findByKey` для защиты от задвоения; операция при пустой конфигурации → понятная ошибка до обращения в ERP.
**Когда готово**: адаптеры создают и удаляют документ в тестовых ERP (ручная проверка на стенде с реальным RemOnline/МойСкладом — один документ, затем удалить); тесты зелёные.

### Фаза 12: Выплата, удаление, ERP для ручных движений (tracer bullet PRD 3)
**Цель**: Полный бэкенд PRD 3: выплата = документ ERP + движение `PAYOUT` атомарно; удаление зеркально; ручные движения с `erpSyncRequired` проходят через тот же механизм.
**Что затрагивает?** backend, contracts
**Задачи:**
- [ ] `POST /v1/{direction}/accounting/payout` — любая сумма `> 0`; при остатке ≤ 0 или сумме > остатка без `confirmNegativeBalance` → ответ «требуется подтверждение» с остатком; порядок: проверка идентичности и конфигурации → ERP create → транзакция (движение `PAYOUT`, `ErpCashDocument`, документы начисления → `PAID` при остатке ≤ 0) → при сбое транзакции компенсация ERP delete, при неудаче компенсации — лог с внешним ID; блокировка по сотруднику
- [ ] `POST .../payout/batch` — по каждому сотруднику на его серверный остаток, список требующих подтверждения и ошибок
- [ ] `DELETE .../payout/:id` и `DELETE .../balance/transactions/:id` (только движения с документом ERP): ERP delete → транзакция (удаление движения, `PAID` → `ACCRUED`); отказ ERP → ничего не изменилось
- [ ] Создание ручного движения с `erpSyncRequired = true` (фаза 7) переводится на тот же порядок «ERP → транзакция → компенсация»; `INCOME`/`OUTCOME` по знаку; сторно для таких движений запрещено
- [ ] `GET /v1/{direction}/accounting/payout/:period` — таблица сотрудников: начислено / авансы / ручные / остаток / выплачено / `payoutStatus`, итоги; переход в `PAID` также при закрытии остатка ручным приходом
**Тесты**: unit/e2e с замоканной ERP: ошибка и таймаут ERP → ни движения, ни документа; успех ERP + инъекция ошибки в БД → компенсация вызвана; `amount ≤ 0` → `400`; без `confirmNegativeBalance` при нулевом остатке → отклонено с остатком, с флагом — создано, баланс в минусе; `PAID` при выплате и при ручном приходе; удаление выплаты возвращает `ACCRUED`; отказ ERP на удалении → всё на месте; две параллельные выплаты → один документ; `service` никогда не пишет в МойСклад; e2e «закрытие → начисление → выплата → удаление».
**Когда готово**: все эндпоинты PRD 3 работают с замоканной ERP и один раз проверены на стенде с реальной; `ENDPOINTS.md` обновлён.

### Фаза 13: Дизайн — выплата зарплаты
**Цель**: Макеты страницы выплаты, формы выплаты и состояний ERP.
**Что затрагивает?** design
**Задачи:**
- [ ] Экран «Выплата зарплаты · Месяц»
- [ ] Drawer «Выплата сотруднику» с предупреждением о нулевом/отрицательном остатке
- [ ] Модалки: подтверждение массовой выплаты, результат, «Удалить выплату», ошибка ERP
- [ ] Дополнение ленты баланса: ссылка на документ ERP, действие «Удалить» у выплаты
- [ ] Записать Node ID в таблицу
**Промты для Pencil**:

> **P3.1 — Выплата зарплаты.** Создай экран «Выплата · Месяц» в стиле `x2soj`: Page Header «Выплата зарплаты» + Direction Tabs (Сервис / Магазин) + Period Chip + подпись «Касса: RemOnline · Основная» для сервиса или «МойСклад · статья «Зарплата»» для магазина (read-only, без выбора). KPI Row: «Остаток к выплате» (сумма положительных остатков), «Выплачено за месяц», «Сотрудников не выплачено», «С отрицательным остатком». Фильтр-чипы: Все / Только невыплаченные / Частично. Таблица с чекбоксами: Сотрудник (инициалы + ФИО + отдел), Начислено, Авансы, Ручные (+/−), Остаток ₽ (минус — красным, ноль — серым), Выплачено за месяц, Статус (бейджи: Не выплачено — серый, Выплачено частично — жёлтый, Выплачено — зелёный), Действия: кнопка «Выплатить» (для всех строк, включая нулевые/отрицательные) и «Открыть баланс». Selection Bar: «Выплатить выбранным (12) · 345 600 ₽». Нарисуй confirm массовой выплаты: список выбранных с суммами, отдельная секция-предупреждение «3 сотрудника с нулевым или отрицательным остатком — выплата уведёт баланс в минус» с чекбоксом «Понимаю, выплатить им остаток/указанную сумму», primary «Выплатить». Модалка результата: «Выплачено 11 из 12», ошибки ERP списком с текстом и кнопкой «Повторить неудачные». Мобильная версия — карточки с кнопкой «Выплатить».

> **P3.2 — Выплата сотруднику.** Drawer (десктоп) / bottom-sheet (мобильный) «Выплата · Иван Петров · Сервис»: сверху компактный KPI «Остаток сейчас 45 000 ₽»; ниже свёрнутая лента баланса за месяц (3–4 строки, «показать все»); поле Сумма (предзаполнено остатком, крупное), дата (сегодня), комментарий; read-only строка «Документ: RemOnline · касса Основная · расход · «Зарплата за июль 2026»»; предпросмотр «Остаток после выплаты: 0 ₽». Состояния: (а) обычное; (б) предупреждение жёлтым alert «Остаток 0 ₽ — после выплаты будет −15 000 ₽» + обязательный чекбокс «Подтверждаю выплату сверх остатка», без него кнопка disabled; (в) сумма больше остатка — тот же alert; (г) ошибка ERP красным alert «RemOnline недоступен: timeout. Выплата не создана, попробуйте ещё раз» с кнопкой «Повторить»; (д) спиннер «Создаём документ в ERP…». Кнопки «Выплатить» / «Отмена».

> **P3.3 — Удаление выплаты и документ ERP в ленте.** На экране «Баланс · Сотрудник» (P2.2) у строки типа «Выплата» покажи колонку ERP с иконкой и внешним ID («RO-48213» / «МС · ордер №117»), при наведении — тултип «Открыть в RemOnline». Действие «Удалить» (иконка корзины, красный). Confirm-модалка «Удалить выплату 45 000 ₽ от 05.08.2026?» с текстом «Будет удалён расходный документ RO-48213 в RemOnline и движение на балансе. Документ начисления вернётся в «Ожидает выплаты»», кнопки «Удалить» (destructive) / «Отмена»; состояние ошибки «RemOnline не позволил удалить документ: … Ничего не изменено» с «Повторить». То же для ручного движения с документом ERP.

**Когда готово**: экраны в `.pen`, Node ID в таблице, скриншоты согласованы.

| Экран | Node ID (десктоп) | Node ID (мобильный) |
|---|---|---|
| Выплата · Месяц | — | — |
| Drawer · Выплата сотруднику | — | — |
| Confirm / результат массовой выплаты | — | — |
| Удаление выплаты + ERP в ленте | — | — |

### Фаза 14: Фронтенд — страница выплаты и форма выплаты
**Цель**: День выплаты проходит из UI по макетам P3.1/P3.2.
**Что затрагивает?** frontend
**Задачи:**
- [ ] Маршрут `/payout`, пункт меню «Выплата» в разделе «Зарплата»; `features/Payout/model/api.ts` — сводка, мутации `payout`/`payoutBatch`, `erp_cash_config` (для подписи кассы/статьи)
- [ ] `pages/Payout` — KPI, фильтры, таблица со статусами и отрицательными остатками, Selection Bar, мобильные карточки; перезапрос после каждого действия (параллельная работа двух руководителей)
- [ ] `features/Payout/ui/PayoutDrawer` — сумма с предзаполнением, свёрнутая лента, предпросмотр остатка, предупреждение + чекбокс подтверждения (`confirmNegativeBalance`), состояния ошибки ERP с «Повторить» и загрузки
- [ ] Confirm массовой выплаты с секцией нулевых/отрицательных остатков и модалка результата с «Повторить неудачные»
- [ ] Та же форма выплаты доступна из `pages/EmployeeBalance`
**Тесты**: компонентные тесты drawer (состояния а–д), таблицы; e2e с замоканной ERP на стенде: выплата полная → статус «Выплачено», частичная, при нулевом остатке с подтверждением, ошибка ERP → ничего не создано.
**Когда готово**: скриншоты совпадают с P3.1/P3.2; сценарии проходят; `build`/`lint` чистые.

### Фаза 15: Фронтенд — удаление выплаты, ERP в ленте и форме движения
**Цель**: Исправление ошибок руководителя и проведение ручных движений через кассу ERP из UI (P3.3, дополнение P2.3).
**Что затрагивает?** frontend
**Задачи:**
- [ ] Колонка ERP в ленте `pages/EmployeeBalance`: внешний ID, ссылка/тултип; действие «Удалить» у выплат и ручных движений с документом ERP вместо «Сторно»
- [ ] Confirm удаления с текстом о документе ERP; обработка отказа ERP («ничего не изменено», «Повторить»); инвалидация баланса, сводки выплаты и документов начисления
- [ ] В `NewTransactionDrawer` (фаза 10): при включённом «Провести в кассе ERP» — подсказка о типе документа по направлению, состояние ошибки ERP, спиннер «Создаём документ в ERP…»
- [ ] «Удалить выплату» также со строки на `pages/Payout`
**Тесты**: компонентные тесты состояний; e2e: создать аванс «через кассу» → в ленте ID документа → удалить → документа и движения нет; удалить выплату → документ начисления снова «Ожидает выплаты».
**Когда готово**: скриншоты совпадают с P3.3; сквозной e2e «закрытие → начисление → выплата → удаление выплаты» проходит в браузере; `build`/`lint` чистые.

---

## Покрытие критериев готовности

| PRD | Критерии | Фазы |
|---|---|---|
| PRD 1 | статус и кнопка на плане продаж, окно подтверждения, неявный синк, ошибка синка, уволенные, раздельное закрытие, сброс кэша, документы = снапшот, нулевые, всё-или-ничего, список/карточка, reopen, статус в отчёте, блокировка часов (5 пунктов), событие, контракты, `ENDPOINTS.md` | 1, 2 (BE); 4, 5 (FE) |
| PRD 2 | проведение/идемпотентность/массовые/статусы/корректировка с `ACCRUAL_ADJUSTMENT`/отмена/`PAID`-запрет; баланс по направлению, `SUM`, типы, комментарии, нет `PATCH`/`DELETE`, задним числом, минус без лимита, `erpSyncRequired`, фильтры, раскрытие, сводка отдела; UI; reopen-тест | 6, 7 (BE); 9, 10 (FE) |
| PRD 3 | сумма любая, `confirmNegativeBalance`, `PAID` по остатку, batch, удаление, нет `PAYOUT_REVERSAL`; ERP по направлению, `INCOME`/`OUTCOME`, всё-или-ничего, компенсация, идентичность, удаление ручного с ERP, назначение, конфигурация кассы/статьи, внешний ID, блокировка; UI; e2e | 11, 12 (BE); 14, 15 (FE) |

## Что требует решения до старта соответствующих фаз

- **Фаза 11 — блокер**: подтвердить в API RemOnline и МойСклада удаление кассового документа/ордера. Без этого сценарий «руководитель ошибся» нереализуем.
- **Фаза 11**: одна статья расходов МойСклада «Зарплата» на все типы движений или отдельные (единственный открытый вопрос PRD 3) — предварительно одна.
- **Фаза 2**: таймаут синка при закрытии (предварительно 2 минуты).
- Ограничение доступа (кто видит действия руководителя) — отдельная задача после фазы 15, в плане не учитывается.
