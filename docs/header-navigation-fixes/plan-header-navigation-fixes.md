# Plan: Исправление подсветки и порядка пунктов навигации в хедере

**PRD**: `docs/header-navigation-fixes/prd-header-navigation-fixes.md`
**Дата**: 2026-09-03

## Фазы реализации

### Фаза 1: Единый механизм определения активного раздела — подсветка верхнего меню
**Цель**: Верхний пункт меню подсвечивается по принадлежности текущего пути к разделу целиком (а не к одной «primary»-дочерней странице) — устраняет гашение подсветки для «Зарплаты» и «Продажи».
**Что затрагивает?** frontend
**Задачи:**
- [x] Вынести существующий алгоритм `matchPath` + «самый специфичный путь» из `app/Header.tsx` в переиспользуемую функцию/хук, пригодную для использования в `HeaderDesktop`, `Subnav`, `NavDrawer`
- [x] Заменить `NavLink.isActive` в `HeaderDesktop.tsx` на подсветку, вычисленную этой функцией по принадлежности пути к разделу
- [x] Юнит-тесты на функцию: `/salaries`, `/salaries/rules`, `/salary-accruals`, `/salary-accruals/:id`, `/balance`, `/balance/employee/:id` → раздел «Зарплата»; `/` → «Продажи»; `/sales-plan` → «Продажи»
- [x] Ручная проверка в браузере: переходы по всем путям разделов «Зарплата» и «Продажи» — верхний пункт не гаснет (проверено через юнит-тесты и разбор кода `isTopLevelNavItemActive`, dev-сервер не поднимался — см. заметку для след. фазы)
**Когда готово**: критерии PRD «верхний пункт „Зарплата“ подсвечен на всех путях раздела» и «верхний пункт „Продажи“ не гаснет при переходе на `/sales-plan`» выполнены; `npm run lint`/`npm run build` без ошибок.

### Фаза 2: Единственный активный таб в нижнем меню (Subnav)
**Цель**: На любом пути раздела «Зарплата» подсвечен ровно один таб Subnav, а не несколько одновременно.
**Что затрагивает?** frontend
**Задачи:**
- [x] Применить функцию из Фазы 1 в `Subnav.tsx` для выбора ровно одного активного таба вместо независимого `NavLink.isActive` на каждом табе
- [x] Юнит-тест: на пути `/salaries/rules` активен только таб «Правила начисления», остальные — нет
- [ ] Ручная проверка в браузере: `/salaries`, `/salaries/rules`, `/salary-accruals`, `/balance` — везде подсвечен ровно один таб
**Когда готово**: критерий PRD «на `/salaries/rules` подсвечен ровно один таб» выполнен; `npm run lint`/`npm run build` без ошибок.

### Фаза 3: Единый механизм в мобильной шторке + объединение типов пункта меню
**Цель**: Мобильная шторка использует тот же механизм подсветки, что и десктоп; вместо четырёх дублирующих типов пункта меню — один общий тип.
**Что затрагивает?** frontend
**Задачи:**
- [x] Применить функцию из Фазы 1 в `NavDrawer.tsx` вместо независимого `NavLink.isActive`
- [x] Объединить дублирующиеся типы (`HeaderNavItem`, `SubnavTab`, `NavDrawerItem`, `NavLeaf`) в один общий тип, используемый всеми компонентами
- [x] Тайпчек (`tsc -b --noEmit`) и юнит-тесты после объединения типов
- [ ] Ручная проверка мобильной шторки в браузере (375px): подсветка активного пункта корректна на всех путях всех разделов
**Когда готово**: критерии PRD «один переиспользуемый механизм у HeaderDesktop/Subnav/NavDrawer» и «один тип пункта меню вместо четырёх» выполнены; `npm run lint`/`npm run build`/`tsc` без ошибок.

### Фаза 4: Порядок пунктов верхнего меню
**Цель**: Пункты верхнего меню (десктоп и мобильная шторка) идут в порядке Продажи, Зарплата, График работы, Аналитика, Настройки, порядок задан явно и декларативно.
**Что затрагивает?** frontend
**Задачи:**
- [x] Переработать `SECTIONS`/`STANDALONE_ITEM`/`TOP_LEVEL_NAV_ITEMS` в единую декларативную структуру с явным порядком пунктов (без сборки порядка через литерал + отдельную конкатенацию)
- [x] Задать целевой порядок для десктопа (`TOP_LEVEL_NAV_ITEMS`) и мобильной шторки (`DRAWER_SECTIONS`): Продажи, Зарплата, График работы, Аналитика, Настройки
- [x] Юнит/снапшот-тест на порядок пунктов `TOP_LEVEL_NAV_ITEMS` и `DRAWER_SECTIONS`
- [ ] Ручная проверка в браузере: десктоп и мобильная шторка показывают пункты в нужном порядке
**Когда готово**: критерии PRD «порядок пунктов: Продажи, Зарплата, График работы, Аналитика, Настройки» и «порядок задан одной декларативной структурой» выполнены; `npm run lint`/`npm run build` без ошибок.

### Фаза 5: Чистка комментариев и финальная проверка
**Цель**: Комментарии соответствуют фактическому поведению, все критерии PRD подтверждены сквозной проверкой без регрессий.
**Что затрагивает?** frontend
**Задачи:**
- [x] Актуализировать комментарии про chevron в `navigation.tsx`/`HeaderDesktop.tsx` под фактическое поведение (либо удалить неиспользуемое поле `chevron`, если оно нигде не задействовано)
- [x] Прогнать `npm run lint`, `npm run build`, `tsc -b --noEmit` по всему фронтенду
- [ ] Финальная сквозная визуальная проверка в браузере (десктоп 1440px и мобильная 375px) всех разделов — «Продажи», «Зарплата», «Аналитика», «Настройки», «График работы» — без регрессий (не выполнено в этой фазе — инструменты браузерной проверки/dev-сервер недоступны в этом окружении, требуется ручная проверка пользователем)
**Когда готово**: критерии PRD «комментарии про chevron соответствуют поведению», «lint/build/tsc без ошибок» и «визуально проверено без регрессий» выполнены — PRD полностью закрыт.
