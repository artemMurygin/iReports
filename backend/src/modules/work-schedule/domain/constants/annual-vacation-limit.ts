// Годовой лимит отпуска — единая константа для всех сотрудников (PRD,
// "Технические ограничения": "Годовой лимит отпуска (31 день в дизайне) —
// единый для всех сотрудников, хранится как константа/настройка, не поле
// сотрудника"). Ни модели сотрудника (BitrixEmployee), ни отдельной
// сущности-настройки для него в этой итерации нет: значение одно на всю
// компанию и не меняется по сотруднику/отделу/году, поэтому достаточно
// именованной константы в домене модуля, а не строки в БД.
export const ANNUAL_VACATION_DAYS_LIMIT = 31;
