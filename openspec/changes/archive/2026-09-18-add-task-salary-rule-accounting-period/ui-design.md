# UI Design: add-task-salary-rule-accounting-period

## Источник дизайна
- **.pen файл**: `design/sallary-first-iteration.pen`
- **UI Kit**: фрейм `uDEum` в этом же файле

---

## Экраны

Новых/изменённых Pages из `architecture.md` нет — `pages/SalaryRules` и `pages/SalaryRuleDetail` не
меняются на уровне страницы. По явному решению пользователя (изменение — одно новое поле внутри уже
существующей карточки правила `TaskCompletion`, шаг 2 мастера, фрейм `EdCuh`) отдельный Pencil-макет
для этого change не создаётся.

| Page (из architecture.md) | Имя фрейма в .pen | Node ID | Breakpoints (1440 / 390) | Переиспользованные компоненты UI Kit | Статус (draft/ready) |
|---|---|---|---|---|---|
| — | — | — | — | — | — |

## Ключевые состояния экранов

Нет состояний, отдельно влияющих на разметку сверх обычного состояния карточки правила
`TaskCompletion` (уже спроектированного во фрейме `EdCuh`).

| Экран | Состояние | Как показано (отдельный артборд / вариант компонента) | Node ID (если отдельный артборд) |
|---|---|---|---|
| — | — | — | — |

## Новые компоненты UI Kit

Не требуются — новое поле рендерится уже существующим компонентом `PeriodPicker`
(`shared/ui-kit/organisms/PeriodPicker.tsx`), без изменений в самом компоненте.

| Компонент | Node ID (внутри uDEum) | Где используется | Почему существующего компонента недостаточно |
|---|---|---|---|
| — | — | — | — |

## Отклонения от architecture.md

По явному решению пользователя формальный Pencil-макет для этого change не создавался — фича
ограничивается добавлением одного поля (`PeriodPicker`, уже существующий UI Kit компонент) в уже
спроектированную карточку `TaskCompletion` (фрейм `EdCuh`, `design/sallary-first-iteration.pen`).
Реализация (`tasks.md`) размещает новый блок в `TaskCompletionRuleFields.tsx` визуально согласованно с
существующей карточкой (отступы/типографика/токены — как у соседних полей той же карточки, например
блока «Периодичность»), без отдельной верификации через `TakeScreenshot` в Pencil.

---

## Confirmation Checklist

- [x] Спроектированы все Pages из architecture.md — новых/изменённых Pages нет
- [x] Везде, где был подходящий компонент UI Kit — используется `PeriodPicker` (существующий, без создания нового)
- [x] Новых компонентов не создавалось
- [x] Отдельного Pencil-макета нет — по явному решению пользователя (см. «Отклонения»)
- [x] Пользователь подтвердил пропуск Pencil-дизайна для этого change
- [ ] Пользователь подтвердил переход к tasks.md
