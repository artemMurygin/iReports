<!--
  Artifact: tasks
  Requires: proposal, design, architecture, ui-design

  Инструкция для агента (см. также комментарий-инструкция openspec):
  - Реализация ведётся по TDD. Для каждой содержательной единицы функциональности
    (юзкейс/сервис на бэкенде, компонент/хук с логикой на фронте) — НЕ для чисто
    визуальной вёрстки без ветвлений/данных/состояний — группа задач состоит из
    4 ОТДЕЛЬНЫХ пунктов чек-листа в этом порядке: 1) написать тест(ы) на ожидаемое
    поведение — ДО реализации; 2) прогнать их и зафиксировать red по ожидаемой
    причине; 3) реализовать функциональность; 4) прогнать тесты снова и
    зафиксировать green + отсутствие регрессий в соседних тестах. Не сворачивай
    эти 4 шага в один пункт.
  - Для задач без логики (чисто визуальная вёрстка) шаги 1/2/4 можно опустить, но
    это исключение нужно явно обосновать в тексте задачи, а не молча пропустить.

  Контекст, важный для исполнителя (найден при подготовке этого файла, не
  описан в proposal/design/architecture.md — фиксирую здесь, чтобы не потерялся):
  - В истории репозитория уже была фича с очень похожим названием и полностью
    удалена по запросу пользователя: миграция `20260811010000_add_task_completion`
    создавала таблицу `task_completions` и тип правила `'TaskCompleted'`, миграция
    `20260904122407_drop_task_completions` (за 3 дня до этого change) полностью их
    удалила («Удаление зарплатного правила TaskCompleted и временного воркфлоу
    TaskCompletion — полное удаление фичи по запросу пользователя»). Проверено:
    в текущем коде (`backend/src/**`, `contracts/**`, `frontend/src/**`) не осталось
    ссылок ни на `TaskCompleted`, ни на `task_completions` — только в истории
    миграций (ожидаемо, не трогать) и в seed-данных снапшотов закрытых периодов
    (`prisma/seed-data/*.json` — исторические демо-данные, не трогать). Новый тип
    правила в этом change называется `TaskCompletion` (не `TaskCompleted`) и
    реализован принципиально иначе (через реальную интеграцию с Bitrix24
    Tasks API, а не отдельный "воркфлоу") — переиспользовать старые
    имена/таблицы не нужно, конфликта имён с БД нет (таблица уже удалена).
  - На отдельной, НЕ слитой в `feat/salary` git-ветке/worktree
    (`feat/task-rule-archiving-and-links`) обнаружена параллельная незавершённая
    попытка реализации похожей фичи в `domains/shop/modules/accounting/**` и
    `contracts/commands/task-completion.ts`. Эта ветка вне текущего состояния
    репозитория и НЕ является основой для задач ниже — architecture.md/design.md
    этого change осознанно проектировались заново (в т.ч. явно отвергли
    отдельный класс `BitrixTasksClient`). Раздел не заводит задачу на её ревью —
    если пользователь захочет свести две ветки, это отдельное решение вне
    скоупа tasks.md.
  - `ui-design.md` явно фиксирует, что макетов для `SalaryAccrualDocument`
    (модалка ручного ввода суммы) и `SalaryReportV2` (бейдж статуса задачи) в
    этой итерации нет («нужен отдельный проход по ui-design, если требуется
    макет»). Соответствующие задачи ниже (24, 26) реализуют эти элементы из
    существующих примитивов UI Kit по прецеденту `AdjustLineModal.tsx`/
    `AccrualStatusBadge.tsx` (тот же приём, что уже применён в проекте для
    экранов без Pencil-фрейма, например `pages/Login`/`pages/OAuthCallback` в
    change `add-bitrix24-auth-and-rbac`) — без точного визуального макета,
    это явное, осознанное отклонение, а не пропуск.
-->

## 1. Prisma: `SalaryTask` + расширение `SalaryAccrualLine`

- [x] 1.1 Создать `backend/prisma/schema/salary-task.prisma` — новая общая для `service`/`shop` модель `SalaryTask` (`@@map("salary_tasks")`) по паттерну "Общие таблицы между service и shop" (`backend/CLAUDE.md`): `id String @id @default(uuid())`, `salaryRuleId String` (FK-подобная ссылка на `SalaryRule.id`, без физического `@relation` — по прецеденту `SalaryAccrualLine.ruleId`, который тоже не FK), `direction String`, `period String` (`YYYY-MM`), `deadline DateTime`, `isRecurring Boolean`, `bitrixTaskId String`, `taskStatus String` (сырой код статуса Bitrix24), `lastSyncedAt DateTime?`, `createdAt`/`updatedAt`. Уникальный индекс `@@unique([salaryRuleId, period])` (design.md Decision 1/4 — идемпотентность `ensure()`). Verify: `npx prisma validate --config prisma.config.ts` проходит.
- [x] 1.2 Расширить существующую модель `SalaryAccrualLine` в `backend/prisma/schema/salary-accrual.prisma` двумя аддитивными nullable-полями: `comment String?` (первичный комментарий руководителя при ручном вводе, design.md Decision 5 — отдельно от `SalaryAccrualLineAdjustment`, который остаётся историей корректировок УЖЕ проведённой суммы) и `requiresManualInput Boolean @default(false)`. Verify: `npx prisma validate --config prisma.config.ts` проходит.
- [x] 1.3 Сгенерировать и применить миграцию (`npx prisma migrate dev --config prisma.config.ts --name add_salary_task`) — аддитивная, не трогает существующие таблицы кроме двух новых колонок `SalaryAccrualLine`. Verify: `npx prisma migrate dev` создаёт файл в `backend/prisma/migrations/`, применяется без ошибок; `npx prisma generate --config prisma.config.ts` обновляет клиент в `prisma/generated/prisma/schema` без ошибок типов.

## 2. Contracts (`ireports-contracts`): схемы `TaskCompletion` и связанных ответов

- [x] 2.1 В `contracts/commands/salary-rule.ts` (направление `service`) добавить `taskCompletionSalaryConfigSchema` (`bitrixTaskTitle: z.string()`, `taskDescription: z.string().optional()`, `isRecurring: z.boolean()`, `deadlineTemplate: z.string()` — ISO-дата: для разового правила берётся буквально, для регулярного используется только число месяца, см. задачу 10.3) и `taskCompletionSalaryRuleSchema` (`type: z.literal('TaskCompletion')`, `name`, `targetRole: targetRoleSchema`, `config: taskCompletionSalaryConfigSchema`); добавить оба варианта в `salaryRuleRequestSchema`/`salaryRuleResponseSchema` (`z.discriminatedUnion('type', [...])`) и экспортировать `taskCompletionSalaryConfigSchema`. Verify: `cd contracts && npx tsc --noEmit` проходит.
- [x] 2.2 Зеркально (независимая копия, issue #57) добавить `taskCompletionShopSalaryConfigSchema`/`taskCompletionShopSalaryRuleSchema` в `contracts/commands/shop-salary-rule.ts`, включить в `shopSalaryRuleRequestSchema`/`shopSalaryRuleResponseSchema`. Verify: `npx tsc --noEmit` в `contracts/`.
- [x] 2.3 Расширить `salaryAccrualLineSchema` в `contracts/commands/salary-accrual.ts` полями `comment: z.string().nullable()` и `requiresManualInput: z.boolean()` (зеркалят новые Prisma-колонки задачи 1.2); добавить `setTaskCompletionLineRewardRequestSchema` (`amount: z.number().int(), comment: z.string().min(1)`) и экспортировать. Verify: `npx tsc --noEmit` в `contracts/`.
- [x] 2.4 Собрать пакет (`npm run build` в `contracts/`, если есть отдельный шаг сборки — проверить `contracts/package.json`) и убедиться, что backend/frontend видят новые типы через workspace-зависимость (`npx tsc --noEmit` в `backend/` пока будет падать до задач 3+ — это ожидаемо и фиксируется как red в разделе 3/4). Verify: сборка `contracts/` без ошибок.

## 3. Cross-cutting: `SalaryRule.calculate()` → `CalculationLine | null` (направление `service`) (TDD)

Причина: до этого change ни один `SalaryRule.calculate()` не возвращал `null` — `CalculationLine[]` в `PeriodCalculationOrchestrator`/`buildRuleBreakdown`/`buildSalaryReportRules` сопоставлялись с `rules[]` строго по индексу. `TaskCompletion.calculate()` обязан возвращать `null`, пока связанная задача не «Выполнено» (specs `service/accounting`, `shop/accounting`: «строка отсутствует в отчёте»), поэтому вся цепочка ниже должна научиться работать с "дырами" ДО того, как заводить сам `TaskCompletion` (иначе тесты 10.x/16.x нечем будет проверить сквозно).

- [x] 3.1 Написать тесты: `PeriodCalculationOrchestrator.calculate()` (`backend/src/domains/service/modules/accounting/domain/services/period-calculation.orchestrator.ts`) сохраняет `null` на позиции правила, чьё `calculate()` вернул `null`, не выбрасывая исключение и не схлопывая массив; `PeriodCalculationOrchestrator.total()` суммирует `line?.amount ?? 0`, игнорируя `null`; `buildRuleBreakdown()` (`domain/services/rule-breakdown.builder.ts`) возвращает МЕНЬШЕ элементов, чем `rules.length`, пропуская правила с `null`-строкой (не вставляя "пустую" строку на их место); `buildSalaryReportRules()` (`application/mappers/salary-report/to-salary-report-rules.ts`) не включает в `rules[]` ответа правило, для которого брейкдаун отсутствует хотя бы в одном из режимов (FACT или PROGNOSE), сопоставляя факт/прогноз по `ruleId`, а не по позиции массива. Verify: тесты видны раннеру (`npm run test -- --testPathPatterns=domains/service/modules/accounting`).
- [x] 3.2 Прогнать тесты из 3.1, зафиксировать red (текущая реализация либо падает на `null.amount`, либо вставляет запись с `undefined`-полями вместо пропуска).
- [x] 3.3 Реализовать: (a) `SalaryRule.calculate()` в `domain/types/salary-rule.types.ts` — сигнатура `CalculationLine | null | Promise<CalculationLine | null>`; (b) `PeriodCalculationOrchestrator.calculate()` — тип возврата `Promise<(CalculationLine | null)[]>`, без изменения порядка `push`; `total()` — `sum + (line?.amount ?? 0)`; (c) `buildRuleBreakdown()` — `rules.flatMap((rule, index) => { const line = lines[index]; return line ? [{...}] : []; })`; (d) `buildSalaryReportRules()` — вместо `rules.map((rule, index) => ...)` построить `Map<ruleId, RuleBreakdownLine>` из `factBreakdown`/`prognoseBreakdown` и итерировать по `rules`, пропуская (`continue`/`filter`) те, где хотя бы одна из двух карт не содержит `ruleId`. Проверить и обновить ВСЕХ остальных потребителей `PeriodCalculationOrchestrator.calculate`/`buildRuleBreakdown`/`.total()` (`grep -rn "PeriodCalculationOrchestrator\|buildRuleBreakdown" backend/src/domains/service/modules/accounting`) — как минимум снапшот при закрытии периода (`AccountingPeriodClosedEventHandler`/`CloseAccountingPeriodHandler`, если он использует эти функции напрямую) должен получить то же поведение (null-строки не попадают в снапшот). Существующие типы правил (`PayPerHour`/`ServiceCompleted`/`OrderPayed`) продолжают всегда возвращать не-`null` `CalculationLine` — их поведение не меняется.
- [x] 3.4 Прогнать тесты из 3.1 и полный `npm run test -- --testPathPatterns=domains/service/modules/accounting` — зафиксировать green, регрессий в существующих тестах `PayPerHour`/`ServiceCompleted`/`OrderPayed`/отчётов нет.

## 4. Cross-cutting: то же самое для направления `shop` (TDD)

- [x] 4.1 Написать зеркальные тесты для `backend/src/domains/shop/modules/accounting/domain/services/period-calculation.orchestrator.ts`, `rule-breakdown.builder.ts`, `application/mappers/salary-report/to-salary-report-rules.ts` — та же логика: `null`-строка не ломает суммирование, брейкдаун и отчёт пропускают правило целиком. Verify: тесты видны раннеру (`--testPathPatterns=domains/shop/modules/accounting`).
- [x] 4.2 Прогнать тесты из 4.1, зафиксировать red.
- [x] 4.3 Реализовать тот же набор изменений (шоп-версии классов, независимые от `service`, issue #57) в `domain/types/salary-rule.types.ts`, `PeriodCalculationOrchestrator`, `rule-breakdown.builder.ts`, `to-salary-report-rules.ts` направления `shop`; проверить прочих потребителей (`grep -rn "PeriodCalculationOrchestrator\|buildRuleBreakdown" backend/src/domains/shop/modules/accounting`), включая снапшот закрытия периода `shop`.
- [x] 4.4 Прогнать тесты из 4.1 и полный `npm run test -- --testPathPatterns=domains/shop/modules/accounting` — зафиксировать green, регрессий нет.

## 5. Общая инфраструктура Bitrix: write-методы на существующем `BitrixService` (TDD)

Не заводить новый класс `BitrixTasksClient`/`BitrixTasksService` — расширяется `backend/src/integrations/bitrix/bitrix.service.ts` (design.md Decision 2). Пространство методов — актуальное `tasks.task.*`, не deprecated `task.item.*`.

- [x] 5.1 Написать тесты (`bitrix.service.spec.ts`, мок `BitrixHttpService`/axios) на новые методы `BitrixService`: `createTask({ responsibleBitrixUserId, title, description, deadline }): Promise<{ bitrixTaskId: string }>` — POST `tasks.task.add`, тело провалидировано перед отправкой (по образцу `CustomApiRoappService.createService` — zod-парсинг payload → `BadRequestException` на невалидный вход, `.post(...)` → парсинг ответа → `BadGatewayException` на любую сетевую/HTTP-ошибку, БЕЗ ретраев через `_getWithRetry` — задача может задвоиться при повторном создании, см. design.md); `closeTask(bitrixTaskId): Promise<void>` — `tasks.task.update` со статусом "завершена"/архивной; `updateTaskDeadline(bitrixTaskId, deadline): Promise<void>` — `tasks.task.update` с новым `DEADLINE`; `fetchTaskStatusesBatch(bitrixTaskIds: string[]): Promise<Map<string, TaskStatus>>` — ОДИН batch-запрос (использовать батч-механизм Bitrix24 REST `batch`, по аналогии с постраничной выгрузкой `_fetchDeals`, а не N вызовов `tasks.task.get`), пустой массив на входе не делает сетевой вызов. Verify: тесты видны раннеру.
- [x] 5.2 Прогнать тесты из 5.1, зафиксировать red (методов ещё нет).
- [x] 5.3 Реализовать `createTask`/`closeTask`/`updateTaskDeadline`/`fetchTaskStatusesBatch` в `BitrixService`, новые типы ответов Bitrix Tasks API в `bitrix-api.types.ts` (или zod-схему в `schema.ts`, по актуальному в файле стилю). `RESPONSIBLE_ID` — сотрудник, на которого оформлено правило; `CREATED_BY` — технический пользователь вебхука (design.md Decision 2). Перед реализацией сверить точный формат полей `tasks.task.add`/`tasks.task.update`/`tasks.task.get`/`batch` через `mcp__claude_ai_Bitrix_24__bitrix-method-details` (доступен в этой среде) — не гадать по памяти формат `fields`/`STATUS`-кодов.
  - ⚠️ MCP-инструмент `mcp__claude_ai_Bitrix_24__bitrix-method-details` в этой среде был недоступен (сетевая ошибка `getaddrinfo ENOTFOUND mcp-proxy.anthropic.com`, подтверждено повторной попыткой) — формат полей `tasks.task.add`/`tasks.task.update`/`batch` реализован по документированному публичному API Bitrix24 (UPPERCASE-поля `fields` в запросе, camelCase-поля в ответе `task`, `STATUS = 5` для "Завершена") без сверки через инструмент. Отмечено как открытый вопрос — см. отчёт задачи.
  - `fetchTaskStatusesBatch` возвращает `Promise<Map<string, string>>` (сырой код статуса), а не `Promise<Map<string, TaskStatus>>` буквально из формулировки — доменный VO `TaskStatus` (×2, для `service`/`shop` раздельно) ещё не существует на этом шаге TDD-последовательности (заводится в разделах 9/14) и в любом случае не может быть один класс на оба направления; `BitrixService` — межнаправленческая инфраструктура и не должна знать о доменных типах ни `service`, ни `shop`. Сырой код совпадает по семантике с `SalaryTask.taskStatus String` (задача 1.1: "сырой код статуса Bitrix24").
  - Побочный фикс, необходимый для прохождения тестов: `src/integrations/bitrix/{schema,types,bitrix.module,bitrix.controller}.js` — устаревшие закоммитированные скомпилированные артефакты (единственный коммит `3883417`, до этого change) — удалены, т.к. `moduleFileExtensions: ["js","json","ts"]` в jest-конфиге резолвил их ВМЕСТО актуальных `.ts` (новые экспорты `schema.ts` были невидимы раннеру). `src/app.module.js` — тот же класс проблемы, но вне `src/integrations/bitrix/` и вне скоупа этого раздела — не тронут, зафиксирован как открытый вопрос для отдельной задачи.
- [x] 5.4 Прогнать тесты из 5.1, зафиксировать green, регрессий в существующих read-методах `BitrixService` нет (`npm run test -- bitrix.service`).

## 6. Общая инфраструктура Bitrix: `BitrixTasksGatewayPort`/`Adapter` + DI (TDD)

По структурному образцу `ROAPP_GATEWAY`/`RoappGatewayAdapter` (`backend/src/domains/service/integrations/roapp-gateway/`) — токен объявлен в файле порта, адаптер делегирует в `BitrixService`.

- [x] 6.1 Написать тесты: `BitrixTasksGatewayAdapter` (мок `BitrixService`) делегирует `createTask`/`closeTask`/`updateDeadline` в соответствующие методы `BitrixService` без собственной бизнес-логики. Verify: тест виден раннеру.
- [x] 6.2 Прогнать тест из 6.1, зафиксировать red.
- [x] 6.3 Реализовать `BitrixTasksGatewayPort` (`src/integrations/bitrix/ports/bitrix-tasks-gateway.port.ts` — интерфейс `createTask`/`closeTask`/`updateDeadline` + `export const BITRIX_TASKS_GATEWAY = Symbol('BITRIX_TASKS_GATEWAY')` в этом же файле) и `BitrixTasksGatewayAdapter` (`src/integrations/bitrix/bitrix-tasks-gateway.adapter.ts`); зарегистрировать `{ provide: BITRIX_TASKS_GATEWAY, useClass: BitrixTasksGatewayAdapter }` в `providers` и добавить `BITRIX_TASKS_GATEWAY` в `exports` `bitrix.module.ts`.
- [x] 6.4 Прогнать тест из 6.1, зафиксировать green, регрессий в `BitrixModule` нет (`npm run test -- bitrix`).

## 7. Общая инфраструктура Bitrix: `buildBitrixTaskLink` (TDD)

Единый (не ×2 по направлениям) хелпер — URL задачи не зависит от направления (design.md Decision 7).

- [x] 7.1 Написать тест: `buildBitrixTaskLink(bitrixTaskId)` возвращает корректный URL вида `https://<портал>.bitrix24.ru/company/personal/user/0/tasks/task/view/<id>/` (портал — из того же env, что уже использует `BITRIX24_WEBHOOK_URL`/аналогичная переменная — свериться с `BitrixHttpService`, не хардкодить домен строкой). Verify: тест виден раннеру.
- [x] 7.2 Прогнать тест из 7.1, зафиксировать red.
- [x] 7.3 Реализовать `buildBitrixTaskLink` в `src/integrations/bitrix/bitrix-task-link-builder.ts` (по образцу `erp-order-link-builder.ts`, но вне доменного модуля — направление-агностичная инфраструктура, design.md Decision 7).
- [x] 7.4 Прогнать тест из 7.1, зафиксировать green.

## 8. Общая инфраструктура: `SalaryTaskStatusSyncCron` — поллинг статусов обоих направлений (TDD)

Крон живёт вне `domains/{service,shop}` (`src/sync/bitrix-tasks/`, по аналогии с `src/sync/bitrix/`) и пишет статус НАПРЯМУЮ в общую таблицу `salary_tasks` через `DatabaseService` (как `BitrixSyncService.uploadModifiedDeals`/`ProductFolderTreeService`), а не через доменные `SalaryTaskRepository` каждого направления — обновление `taskStatus`/`lastSyncedAt` не несёт доменной бизнес-логики, поэтому не является нарушением границы `service`/`shop` (см. прецеденты в `backend/CLAUDE.md`, «Общие таблицы», исключения `BalanceTransaction`/`EmployeeDismissal`).

- [x] 8.1 Написать тесты: `SalaryTaskStatusSyncService.run()` читает все `SalaryTask` с не-архивным (нужно определить, что считается "неактивной" задачей — построить критерий из уже известного статуса на момент последнего синка, не архивные/не Done) статусом ОБОИХ направлений одним запросом, вызывает `BitrixService.fetchTaskStatusesBatch(bitrixTaskIds)` ОДНИМ батч-вызовом (не по одному на задачу), апсертит `taskStatus`/`lastSyncedAt` по каждой записи; при сетевой ошибке — не роняет весь цикл, логирует и оставляет `lastSyncedAt` без изменений (следующий тик повторит). Verify: тесты видны раннеру.
- [x] 8.2 Прогнать тесты из 8.1, зафиксировать red.
- [x] 8.3 Реализовать `SalaryTaskStatusSyncService` + `SalaryTaskStatusSyncCron` (`@ProdCron(CronExpression.EVERY_5_MINUTES)`, по образцу `SalesPlanAutoCreationCron`: обернуть вызов в `runInSystemRequestContext` — крон вне HTTP-запроса — и логировать ошибки через `logCronError`) в `src/sync/bitrix-tasks/`, модуль `SalaryTaskStatusSyncModule` (импортирует `BitrixModule` за `BitrixService`), подключить в `app.module.ts`.
  - Критерий "неактивной"/архивной задачи: `taskStatus !== String(BITRIX_TASK_STATUS_COMPLETED)` (сырое, уже сохранённое локально значение) — Bitrix24 Tasks API не выделяет отдельного "архивного" статуса помимо кода "Завершена" (`closeTask` переводит задачу в тот же код 5), поэтому "не архивные" и "не Done" — один и тот же критерий на уровне сырого `taskStatus`.
  - `SalaryTaskStatusSyncService.run()` сам ловит ошибку `fetchTaskStatusesBatch` (не даёт ей всплыть) и логирует через `logCronError`, не трогая `lastSyncedAt`, — иначе тест 8.1 «не роняет весь цикл» непроверяем на уровне сервиса; `SalaryTaskStatusSyncCron` дополнительно оборачивает вызов в try/catch + `logCronError` защитно (по образцу `SalesPlanAutoCreationCron`), хотя `run()` в норме уже не бросает.
- [x] 8.4 Прогнать тесты из 8.1, зафиксировать green, регрессий в `src/sync/bitrix` нет.

## 9. Направление `service`: `SalaryTask` entity + repository (TDD)

- [x] 9.1 Написать тесты: `SalaryTask` (домен, `domain/entities/salary-task/salary-task.entity.ts`) — валидирует обязательность `bitrixTaskId`/`deadline`, метод `markStatus(status: TaskStatus)`; `TaskStatus` VO (`domain/value-objects/task-status.value-object.ts`) — `isDone()` истинен только для кода "Завершена" Bitrix24 (уточнить точный код через `mcp__claude_ai_Bitrix_24__bitrix-method-details` перед написанием теста, не предполагать); `SalaryTaskRepository` (Prisma, `infrastructure/repositories/salary-task/salary-task.repository.ts`) — `findByRuleAndPeriod(ruleId, period)`, `findActiveForDirection(direction)`, `insert`/`save`, ВСЕГДА подставляет/фильтрует `direction: 'service'` (не принимает `direction` параметром снаружи, см. `backend/CLAUDE.md` — изоляция направлений на уровне кода). Verify: тесты видны раннеру.
- [x] 9.2 Прогнать тесты из 9.1, зафиксировать red.
- [x] 9.3 Реализовать `SalaryTask` entity + `TaskStatus` VO (наследует `value-object.base.ts`) + `SalaryTaskRepositoryPort`/токен `SALARY_TASK_REPOSITORY` (`application/ports/salary-task/salary-task.port.ts`) + `SalaryTaskRepository` (Prisma) + маппер (`infrastructure/mappers/salary-task/salary-task.mapper.ts`); зарегистрировать провайдер в `accounting.module.ts`.
- [x] 9.4 Прогнать тесты из 9.1, зафиксировать green.

## 10. Направление `service`: `TaskCompletion` + регистрация (TDD)

- [x] 10.1 Написать тесты (`domain/entities/salary-rules/task-completion.entity.spec.ts`): `TaskCompletion.calculate(context)` возвращает `null`, когда `erpData` не содержит статус связанной задачи ИЛИ статус не Done (spec `service/accounting`: «строка отсутствует в отчёте, пока задача не выполнена»); возвращает `CalculationLine` (`amount: 0`, `requiresManualInput: true`, `sources: [{ type: 'taskCompletion', id: bitrixTaskId, label: <название задачи>, link: buildBitrixTaskLink(bitrixTaskId) }]`) когда статус Done — ВСЕГДА `amount: 0`/`requiresManualInput: true`, независимо от того, была ли уже когда-то введена сумма в документе начисления (сумма живёт только на `SalaryAccrualLine`, не пересчитывается в `calculate()` — см. пояснение в задаче 14, тот же принцип, что и у корректировок `adjust()` других типов правил, которые тоже не влияют на live-пересчёт открытого периода). Verify: тесты видны раннеру.
- [x] 10.2 Прогнать тесты из 10.1, зафиксировать red.
- [x] 10.3 Реализовать `TaskCompletion implements SalaryRule` (по образцу `OrderPayedEntity`: `static create()`, геттеры `name`/`type`/`targetRole`/`config`, приватный `buildSources()`), добавить `['TaskCompletion', TaskCompletion]` в `domain/salary-rule-registry.ts`; расширить `CalculationLine`/`RuleBreakdownLine`/`SalaryAccrualSourceLine` опциональным полем `requiresManualInput?: boolean` (`src/shared/domain/calculation-line.ts` — единственное новое поле в этом уже помеченном как антипаттерн файле, без переноса остального технического долга); erpData-статус читается из нового поля `taskCompletionStatuses?: Record<ruleId, TaskStatus>` (заполняется задачей 12). Обновить `salary-rule-role-catalog.spec.ts` (`listSalaryRuleTypes()` теперь возвращает 4 типа вместо 3 — тест уже существует и содержит хардкод списка, задача явно указана, чтобы не забыть).
- [x] 10.4 Прогнать тесты из 10.1 и весь `salary-rule-registry.spec.ts`/`salary-rule.factory.spec.ts`/`salary-rule-role-catalog.spec.ts`, зафиксировать green, регрессий нет.

## 11. Направление `service`: автосоздание/пересоздание задачи на период (TDD)

По прямому образцу `EnsureSalesPlansForPeriodService`/`SalesPlanAutoCreationCron` (design.md Decision 4).

- [x] 11.1 Написать тесты: `EnsureSalaryTaskForPeriodService.ensure(salaryRuleId, period)` — если `SalaryTaskRepository.findByRuleAndPeriod` уже нашёл запись за этот период, возвращает её без повторного создания задачи в Bitrix24 (идемпотентность, опирается на `@@unique([salaryRuleId, period])` как последний рубеж защиты от гонки, тот же приём, что в `EnsureSalesPlansForPeriodService`); для разового правила (`isRecurring: false`) — не создаёт задачу, если период не совпадает с периодом создания правила (spec: «разовая задача не пересоздаётся»); для регулярного (`isRecurring: true`) — вычисляет `deadline` нового периода из `deadlineTemplate` (число месяца), вызывает `BitrixTasksGatewayPort.createTask(...)`, сохраняет новую `SalaryTask`. Verify: тесты видны раннеру.
  - Сигнатура фактически `ensure(salaryRuleId, period, responsibleBitrixUserId)` — третий параметр не был явно оговорён в тексте задачи, но необходим: `SALARY_RULE_REPOSITORY`/`SalaryTask` не хранят Bitrix-пользователя, а `BitrixTasksGatewayPort.createTask()` требует `responsibleBitrixUserId`. Источник значения — вызывающий код (`GetEmployeeSalaryReportService`/`GetDepartmentSalaryReportService`/`TaskCompletionAutoCreationCron`), который уже знает `CalculationEmployee.id` (Bitrix-сотрудник, см. `calculation-context.ts`) — сервис сам не резолвит сотрудника (не инжектит справочник/`MotivationSchemaRepository`).
  - ⚠️ Открытый вопрос (не решённый design.md/tasks.md): если правило `TaskCompletion` заведено на схему ОТДЕЛА (не личную), `salaryRuleId` один на всех сотрудников отдела, а `@@unique([salaryRuleId, period])` физически допускает только одну задачу на правило за период — значит только первый обработанный сотрудник получит корректный `RESPONSIBLE_ID`, остальные найдут уже существующую запись. Задокументировано в коде `ensure-salary-task-for-period.service.ts`, не устраняется этим разделом.
- [x] 11.2 Прогнать тесты из 11.1, зафиксировать red.
- [x] 11.3 Реализовать `EnsureSalaryTaskForPeriodService` (`application/services/salary-task/`), инжектит `SALARY_TASK_REPOSITORY`, `BITRIX_TASKS_GATEWAY`, `SALARY_RULE_REPOSITORY` (нужен для чтения `TaskCompletionSalaryConfig`/списка активных правил-задач); `TaskCompletionAutoCreationCron` (`infrastructure/cron/`, `@ProdCron` первого числа месяца, `runInSystemRequestContext`, по образцу `SalesPlanAutoCreationCron`); подключить в `accounting.module.ts` (импортировать `BitrixModule`). Добавить ленивый вызов `ensure()` в `GetEmployeeSalaryReportService`/`GetDepartmentSalaryReportService` перед вызовом оркестратора (`@ProdCron` не тикает в dev) — по всем активным регулярным `TaskCompletion`-правилам запрашиваемого сотрудника/отдела за текущий период.
  - `TaskCompletionAutoCreationCron` обходит "список активных правил-задач" через уже существующий `ResolveEmployeeSalaryRulesService.forAllTargets()` (тот же метод, что использует закрытие периода), а не через новый метод `SALARY_RULE_REPOSITORY` — порт этого раздела используется только для чтения config ОДНОГО правила внутри `ensure()` (комментарий в порту явно фиксирует «findAll добавляется, когда появляется конкретный вызывающий код» — таким вызывающим кодом стал уже существующий resolver, а не новый метод порта).
  - Начальный `SalaryTask.taskStatus` сразу после создания — код "2" ("Новая"), подтверждено примером ответа `tasks.task.add` через `mcp__claude_ai_Bitrix_24__bitrix-method-details` (инструмент был доступен в этой сессии, в отличие от раздела 5) — вынесено в `BITRIX_TASK_STATUS_NEW` (`src/integrations/bitrix/schema.ts`).
- [x] 11.4 Прогнать тесты из 11.1, зафиксировать green, регрессий в `salary-report` тестах нет.
  - `npm run test -- --testPathPatterns=domains/service/modules/accounting` — 327/327 зелёных (после устранения разовой флаки e2e `salary-accrual-lines.e2e.spec.ts`, не связанной с этим разделом — см. отчёт задачи); полный `npm run test` — 1412/1412 зелёных (после устранения разовой флаки shop `get-department-salary-report.service.spec.ts`, тоже не связанной с этим разделом).

## 12. Направление `service`: создание/закрытие задачи из правила + erpData (TDD)

- [x] 12.1 Написать тесты: `CreateSalaryRuleHandler.execute()` при `command.rule.type === 'TaskCompletion'` вызывает `BitrixTasksGatewayPort.createTask(...)` ДО записи в БД; при ошибке Bitrix24 — правило НЕ создаётся (design.md Decision 6, «создание правила должно отказывать»); при успехе — `SalaryRule` и новая `SalaryTask` сохраняются в ОДНОЙ транзакции (`UNIT_OF_WORK`, по образцу `CreatePayoutHandler`); если запись в БД падает ПОСЛЕ успешного создания в Bitrix24 — вызывается компенсирующий `closeTask` (design.md Decision 6). `UpdateMotivationSchemaHandler` — при исключении правила `TaskCompletion` из пересобираемого списка (`deleteAllByMotivationSchema` + пересоздание) вызывает `BitrixTasksGatewayPort.closeTask` для каждой удаляемой `TaskCompletion`-задачи ДО удаления записи; сбой вызова Bitrix24 НЕ блокирует удаление правила (design.md Decision 6 — логируется, расхождение обнаруживается кроном 8). `build-service-calculation-context.service.ts` — заполняет `erpData.taskCompletionStatuses` статусами связанных `SalaryTask` для всех `TaskCompletion`-правил схемы. Verify: тесты видны раннеру.
  - Дополнительно (не буквально из формулировки, но необходимо для реального сквозного покрытия): тесты на два новых метода `SalaryTaskRepositoryPort`/`SalaryTaskRepository` (`findManyByRulesAndPeriod` — батч-вход `erpData.taskCompletionStatuses`; `findActiveByRule` — вход `UpdateMotivationSchemaHandler`, вне зависимости от периода, т.к. разовая задача может быть удалена в периоде, отличном от периода её создания) и на `GetDepartmentSalaryReportService` (тот же гейтинг видимости, что и у отчёта сотрудника, но erpData которого собирается вручную, не через `BuildServiceCalculationContextService`, — без этого раздел был бы реализован только для отчёта сотрудника, отчёт отдела остался бы сломан для `TaskCompletion`).
- [x] 12.2 Прогнать тесты из 12.1, зафиксировать red.
  - Зафиксировано: `findManyByRulesAndPeriod`/`findActiveByRule` — `TypeError: ... is not a function` (методов ещё нет); `CreateSalaryRuleHandler`/`UpdateMotivationSchemaHandler` — новые ассершены (`createTask`/`closeTask` не вызваны, транзакция не разветвлена) не проходят, т.к. обработчики ещё не знают о типе `TaskCompletion`; `build-service-calculation-context.service.spec.ts` — `findManyByRulesAndPeriod` не вызывается (сигнатура `build()` без `rules`); `GetDepartmentSalaryReportService` — `erpData.taskCompletionStatuses` не заполняется (строка "выполнена" не появляется в отчёте).
- [x] 12.3 Реализовать: расширить `CreateSalaryRuleHandler` (инжектит `UNIT_OF_WORK`, `MOTIVATION_SCHEMA_REPOSITORY`, `DIRECTORY_REPOSITORY`, `BITRIX_TASKS_GATEWAY`, `SALARY_TASK_REPOSITORY`) веткой для `TaskCompletion`; расширить `UpdateMotivationSchemaHandler` вызовом `closeTask` перед `deleteAllByMotivationSchema`, с try/catch + логированием на несбой основного пути (design.md Decision 6); расширить `BuildServiceCalculationContextService` (принимает `rules` третьим параметром `build()`, инжектит `SALARY_TASK_REPOSITORY`) — при наличии `TaskCompletion`-правил в схеме подтягивает их `SalaryTask.taskStatus` текущего периода через новый `SalaryTaskRepositoryPort.findManyByRulesAndPeriod` (общая функция `buildTaskCompletionStatuses`, переиспользована и в `GetDepartmentSalaryReportService`, чей `erpData` собирается вручную).
  - `responsibleBitrixUserId` для eager-создания задачи (design.md Decision 2) резолвится из `MotivationTarget` схемы: `Employee` → `target.getId()`; `Department` → первый сотрудник отдела (`DirectoryRepositoryPort.findEmployees`) — тот же открытый вопрос, что уже задокументирован в разделе 11 (`@@unique([salaryRuleId, period])` допускает только одну задачу на правило) — не решается заново, только последовательно расширяется на путь создания.
  - Open Question design.md («способ закодировать `salaryRuleId`/период в самой задаче для ручной диагностики в CRM») оставлен НЕ закрытым в этом разделе — `createTask({ description })` передаёт только `config.taskDescription` (авторский текст руководителя), без технического кодирования `salaryRuleId`/периода; отмечено как открытый вопрос в отчёте.
  - Побочный фикс: `CalculateServiceSnapshotRowsService.calculate()` — ещё один (не упомянутый в тексте задачи) вызывающий `BuildServiceCalculationContextService.build()`, найденный при регрессионном прогоне (закрытие периода падало `Cannot read properties of undefined (reading 'filter')`) — обновлён на новую сигнатуру `build(period, employeeId, rules)`.
- [x] 12.4 Прогнать тесты из 12.1 и полный `--testPathPatterns=domains/service/modules/accounting`, зафиксировать green, регрессий нет.
  - `npm run test -- --testPathPatterns=domains/service/modules/accounting` — 345/345 зелёных (64 suites); полный `npm run test` — 1429/1429 зелёных (247 suites). `npx tsc -p tsconfig.build.json --noEmit` — те же 2 ошибки, что и до этого раздела (`salary-accrual.mapper.ts` service/shop, отсутствующие поля `comment`/`requiresManualInput` в мапперах — принадлежат ещё не реализованному разделу 13/18, подтверждено `git stash`/повторным прогоном на состоянии без изменений этого раздела).

## 13. Направление `service`: ручной ввод суммы начисления (TDD)

- [x] 13.1 Написать тесты: `SalaryAccrualLine.setManualReward(amount, comment, setBy)` — только для `requiresManualInput === true` и статуса `DRAFT`, требует непустой `comment` (иначе исключение — spec: «проведение отклоняется, пока комментарий не указан»), устанавливает `amount`/`comment`, сбрасывает `requiresManualInput`; `SalaryAccrualLine.fromBreakdownLine()` копирует новое поле `requiresManualInput` из `SalaryAccrualSourceLine`; `SetTaskCompletionLineRewardHandler.execute({ accrualId, lineId, amount, comment })` (по образцу `AdjustSalaryAccrualLineHandler` — БЕЗ `UNIT_OF_WORK`, одноагрегатное сохранение) находит документ, вызывает `line.setManualReward(...)`, сохраняет через `SALARY_ACCRUAL_REPOSITORY`. Verify: тесты видны раннеру.
  - Сигнатура фактически `setManualReward(amount, comment)` — БЕЗ `setBy`: `setTaskCompletionLineRewardRequestSchema` (задача 2.3, уже реализована) содержит только `amount`/`comment` (комментарий в contracts прямо фиксирует «в отличие от adjustSalaryAccrualLineRequestSchema, здесь нет adjustedBy» — первичный ввод, а не корректировка с автором). Третий параметр `setBy` из формулировки этой задачи не введён — нет ни поля в контракте, ни места для его хранения на `SalaryAccrualLine` (одно поле `comment`, не история записей, как у `SalaryAccrualLineAdjustment.adjustedBy`).
- [x] 13.2 Прогнать тесты из 13.1, зафиксировать red.
  - Зафиксировано: `salary-accrual-line.entity.spec.ts` — `TypeError: line.setManualReward is not a function` (4 из 7 тестов; 3 прошли — они проверяли только `fromBreakdownLine`, ещё не требующий реализации); `set-task-completion-line-reward.handler.spec.ts` — `Cannot find module './set-task-completion-line-reward.handler'` (файла хендлера/команды/exception ещё не существовало).
- [x] 13.3 Реализовать `setManualReward()` на `SalaryAccrualLine` (`domain/entities/salary-accrual/salary-accrual-line.entity.ts`), пробросить `requiresManualInput`/`comment` через маппер (`infrastructure/mappers/salary-accrual/salary-accrual.mapper.ts`) и ответ (`SalaryAccrualLineResponse`, контракт из задачи 2.3); `SetTaskCompletionLineRewardCommand`/`Handler` (`application/command/salary-accrual/set-task-completion-line-reward.{command,handler}.ts`); HTTP-эндпоинт: добавить `lineTaskReward: \`${serviceAccountingRoot}/salary_accruals/:id/lines/:lineId/task-reward\`` в `src/config/app.routes.ts` (зеркало `lineById`), `SetTaskCompletionLineRewardHttpController` (`PATCH`, DTO через `createZodDto(setTaskCompletionLineRewardRequestSchema)`, `@ApiTags('Бухгалтерия: начисления зарплаты')` + `@ApiOperation`, по образцу `adjust-salary-accrual-line.http.controller.ts`); обновить `backend/ENDPOINTS.md`.
  - Новое исключение `SalaryAccrualLineManualInputNotRequiredException` (`domain/exceptions/salary-accrual.exception.ts`, 409) — строка без `requiresManualInput` (не `TaskCompletion`, либо уже введено) отклоняется отдельно от `SalaryAccrualLineNotDraftException`.
  - `SalaryAccrualLineProps.comment`/`requiresManualInput` — сделаны опциональными (не обязательными полями), чтобы не трогать существующие места, где `SalaryAccrualLine` конструируется напрямую без этих полей (`accrue-salary-accrual-line.handler.spec.ts`, `in-memory-salary-accrual.repository.ts`) — геттеры трактуют отсутствие как «не задан»/`false`.
  - `InMemorySalaryAccrualRepository.cloned()` (только для тестов/e2e) не копировал `comment`/`requiresManualInput` при пересборке агрегата на каждое чтение — исправлено, иначе `requiresManualInput` терялся бы после первого `findById` даже без вызова `setManualReward`.
  - Правильный путь ENDPOINTS.md — корень репозитория (`./ENDPOINTS.md`, symlink/ссылка из `backend/CLAUDE.md` как `../ENDPOINTS.md`), не `backend/ENDPOINTS.md` — такого файла не существует, обновлён файл в корне.
- [x] 13.4 Прогнать тесты из 13.1 + e2e (`salary-accrual-lines.e2e.spec.ts`, добавить сценарий 200/400-без-комментария/404), зафиксировать green, регрессий нет.
  - `npm run test -- --testPathPatterns=domains/service/modules/accounting` — 66/66 suites, 359/359 тестов зелёных; полный `npm run test` — 249/249 suites, 1461/1461 тестов зелёных (один инцидент флаки `domains/shop/.../get-department-salary-report.service.spec.ts` при первом полном прогоне — при повторном прогоне и прогоне в изоляции зелёный, это тот же пре-существующий флаки-тест, что уже фиксировался в отчётах разделов 11.4/12.4, не связан с этим разделом — раздел его не трогает).
  - `npx tsc -p tsconfig.build.json --noEmit` — 0 ошибок в `service`/общем коде (единственная оставшаяся ошибка — в `domains/shop/.../salary-accrual.mapper.ts`, принадлежит разделу 18, вне скоупа этого раздела, подтверждено идентичной ошибкой уже в отчёте задачи 12.4).

## 14. Направление `shop`: `ShopSalaryTask` entity + repository (TDD)

Зеркало раздела 9, полностью независимые классы (issue #57, `domains/shop/CLAUDE.md` — префикс `Shop`/`SHOP_` в именах классов/токенов, не в именах файлов).

- [x] 14.1 Написать тесты для `ShopSalaryTask` (`domains/shop/modules/accounting/domain/entities/salary-task/salary-task.entity.ts`), `ShopTaskStatus` VO, `ShopSalaryTaskRepository` (Prisma, всегда `direction: 'shop'`) — та же семантика, что 9.1. Verify: тесты видны раннеру.
- [x] 14.2 Прогнать тесты из 14.1, зафиксировать red.
- [x] 14.3 Реализовать `ShopSalaryTask`/`ShopTaskStatus`/`ShopSalaryTaskRepositoryPort` (токен `SHOP_SALARY_TASK_REPOSITORY`)/`ShopSalaryTaskRepository`/маппер; зарегистрировать в `ShopAccountingModule`.
  - Код "5" ("Завершена") в `ShopTaskStatus.isDone()` продублирован (не импортирован) из `BITRIX_TASK_STATUS_COMPLETED` (`src/integrations/bitrix/schema.ts`, раздел 5) — domain-слой не импортирует инфраструктуру (`backend/CLAUDE.md`). Проверено через `mcp__claude_ai_Bitrix_24__bitrix-method-details`/`bitrix-search` (доступен в этой сессии, в отличие от раздела 5) — официальная REST-документация не публикует явную таблицу «код → название» для `STATUS`, только отдельные примеры и метод `tasks.task.complete` (переводит именно в статус «Завершена»); код "5" переиспользован из уже принятого в разделе 5 решения для консистентности.
- [x] 14.4 Прогнать тесты из 14.1, зафиксировать green.

## 15. Направление `shop`: `TaskCompletionShop` + регистрация (TDD)

Зеркало раздела 10.

- [x] 15.1 Написать тесты (`task-completion.entity.spec.ts` в `domains/shop/.../salary-rules/`): `TaskCompletionShop.calculate()` — та же логика `null`/`requiresManualInput` через `erpData.taskCompletionStatuses`, что и 10.1. Verify: тесты видны раннеру.
- [x] 15.2 Прогнать тесты из 15.1, зафиксировать red.
- [x] 15.3 Реализовать `TaskCompletionShop implements ShopSalaryRule`, добавить `['TaskCompletion', TaskCompletionShop]` в `shopSalaryRuleRegistry`; проверить/обновить `salary-rule-role-catalog` шопа (если есть аналогичный хардкод списка типов в тестах, по прецеденту 10.3).
- [x] 15.4 Прогнать тесты из 15.1 и `shopSalaryRuleRegistry.spec.ts`/`ShopSalaryRuleFactory.spec.ts`, зафиксировать green.

## 16. Направление `shop`: автосоздание/пересоздание задачи на период (TDD)

Зеркало раздела 11.

- [x] 16.1 Написать тесты для `EnsureShopSalaryTaskForPeriodService.ensure(salaryRuleId, period)` — та же идемпотентность/логика разовая-vs-регулярная, что 11.1. Verify: тесты видны раннеру.
- [x] 16.2 Прогнать тесты из 16.1, зафиксировать red.
- [x] 16.3 Реализовать `EnsureShopSalaryTaskForPeriodService`, `ShopTaskCompletionAutoCreationCron` (по образцу `ShopSalesPlanAutoCreationCron`), ленивый вызов в `GetShopEmployeeSalaryReportService`/`GetShopDepartmentSalaryReportService`; подключить `BitrixModule` в `ShopAccountingModule`.
  - Сигнатура `ensure()` расширена третьим параметром `responsibleBitrixUserId: number` относительно буквальной формулировки 16.1/задачи — обнаружено при реализации: `SHOP_SALARY_RULE_REPOSITORY`/`ShopSalaryRule` не хранят `motivationSchemaId`/Bitrix ID сотрудника, а RESPONSIBLE_ID для `createTask()` (design.md Decision 2) обязателен. Решение синхронизировано с параллельно реализованным зеркальным сервисом направления service (раздел 11, `EnsureSalaryTaskForPeriodService.ensure(salaryRuleId, period, responsibleBitrixUserId)`, появившимся в кодовой базе во время работы над этим разделом): вызывающий код (report-сервисы/крон) уже знает `employeeId` = Bitrix ID (см. `CalculationEmployee.id`) и передаёт его явно, без обхода мотивационных схем внутри `ensure()`. `ShopTaskCompletionAutoCreationCron` соответственно получает список правил через уже существующий `ResolveShopEmployeeSalaryRulesService.forAllTargets()` (как и у зеркального крона), а не через новый метод репозитория. Также выявлен и исправлен попутный пробел раздела 15: `shopSalaryRuleConfigSchemaByType` (`infrastructure/schemas/salary-rule.schema.ts`) не содержал записи `TaskCompletion` — без неё чтение ЛЮБОГО персистентного правила `TaskCompletion` через `ShopSalaryRuleMapper.toDomain()` падало.
- [x] 16.4 Прогнать тесты из 16.1, зафиксировать green, регрессий в shop `salary-report` тестах нет.

## 17. Направление `shop`: создание/закрытие задачи из правила + erpData (TDD)

Зеркало раздела 12.

- [x] 17.1 Написать тесты для `CreateShopSalaryRuleHandler`/`UpdateShopMotivationSchemaHandler` (та же логика создания-до-записи-с-компенсацией и закрытия-при-удалении, что 12.1) и `BuildShopCalculationContextService.erpData.taskCompletionStatuses`. Verify: тесты видны раннеру.
- [x] 17.2 Прогнать тесты из 17.1, зафиксировать red.
- [x] 17.3 Реализовать зеркальные изменения в `CreateShopSalaryRuleHandler`, `UpdateShopMotivationSchemaHandler`, `BuildShopCalculationContextService`.
- [x] 17.4 Прогнать тесты из 17.1 и полный `--testPathPatterns=domains/shop/modules/accounting`, зафиксировать green.

## 18. Направление `shop`: ручной ввод суммы начисления (TDD)

Зеркало раздела 13.

- [x] 18.1 Написать тесты для `ShopSalaryAccrualLine.setManualReward(...)` и `SetShopTaskCompletionLineRewardHandler`. Verify: тесты видны раннеру.
- [x] 18.2 Прогнать тесты из 18.1, зафиксировать red.
- [x] 18.3 Реализовать `setManualReward()` на `ShopSalaryAccrualLine`, `SetShopTaskCompletionLineRewardCommand`/`Handler`; добавить `shopAccountingRoot`-зеркало маршрута `lineTaskReward` (см. 13.3) в `app.routes.ts` (`shop.accounting.salaryAccruals.lineTaskReward`), `SetShopTaskCompletionLineRewardHttpController` с `@ApiTags`/`@ApiOperation`; обновить `ENDPOINTS.md`.
- [x] 18.4 Прогнать тесты из 18.1 + e2e, зафиксировать green.

## 19. Backend: сквозная проверка

- [x] 19.1 Прогнать полный backend test suite (`npm run test`) — все зелёные, регрессий нет.
- [x] 19.2 Прогнать `npm run test:e2e` — все зелёные.
- [x] 19.3 `npm run build` (backend) без ошибок типов; `npx eslint --config eslint.config.mjs` без новых ошибок на изменённых файлах.
- [x] 19.4 Проверить `backend/src/config/swagger.config.ts` — `AccountingModule` (`service`)/`ShopAccountingModule` уже включены в `include` соответствующих документов (модули не новые, добавление метода в существующий контроллер Swagger не требует правки этого файла) — задача-проверка, не изменение, если модули уже присутствуют.

## 20. Frontend: `SalaryRuleForm` (`service`) — тип правила `TaskCompletion` (TDD + UI по фрейму `wV3fv`/`aS8yc`)

- [x] 20.1 Написать тесты: `resolveRuleDraft()`/`draftFromRule()` (`frontend/src/features/SalaryRuleForm/service/model/ruleFormSchema.ts`) — ветка `case 'TaskCompletion':` строит/парсит `config` (`bitrixTaskTitle`, `taskDescription`, `isRecurring`, `deadlineTemplate`) и успешно валидируется через `salaryRuleRequestSchema` из задачи 2.1 в обе стороны (draft → request и обратно при редактировании). Verify: `npm run test -- ruleFormSchema` (frontend).
- [x] 20.2 Прогнать тесты из 20.1, зафиксировать red.
- [x] 20.3 Реализовать: добавить `TaskCompletion` в `RULE_TYPE_ORDER`/`RULE_TYPE_LABELS` (`kernel/ruleTypeLabels.ts`)/`SERVICE_RULE_FORM_CONFIG` (`service/model/ruleTypes.ts`), добавить поля черновика (`bitrixTaskTitle`, `taskDescription`, `isRecurring`, `deadlineTemplate`) в `model/ruleDraft.ts` (`createRuleDraft`/`resetAwardFields`), реализовать ветки в `resolveRuleDraft`/`draftFromRule`.
- [x] 20.4 Прогнать тесты из 20.1, зафиксировать green, регрессий в тестах `PayPerHour`/`ServiceCompleted`/`OrderPayed` веток той же функции нет.
- [x] 20.5 Реализовать `TaskCompletionRuleFields` (`ui/RuleFormCard/ui/TaskCompletionRuleFields.tsx`) — поля «Название задачи (в Bitrix24)» (`ERP/Molecule/Field`, node `PM7Om`), «Периодичность» (`ERP/Molecule/Tabs`, node `wQOPI`, третий таб выключен), «Дедлайн» (по паттерну `vm91M`), «Описание задачи» (`ERP/Atom/Textarea`, node `DcWkE`) — прочитать фрейм `wV3fv` (десктоп, `design/sallary-first-iteration.pen`, через `mcp__pencil__execute`/`Get`) для точной композиции блока и сверить вёрстку со скриншотом. Условный рендер этого блока в `RuleFormCard.tsx`/`useRuleFormCard.ts` — добавить `showTaskFields` булев флаг по образцу `showCategory`/`showOrderTypeIds`. Чисто визуальная сборка из готовых атомов `uDEum` (ui-design.md: «новых компонентов не потребовалось») — логика ветвления/данных уже покрыта тестами 20.1, отдельных тестов на сам блок вёрстки не заводится (обоснование).
- [x] 20.6 Реализовать мобильный вариант по фрейму `aS8yc` (`Sheet Header`/`Body`/`Sheet Actions`, 390px) — переиспользует те же поля/логику `TaskCompletionRuleFields` в мобильной компоновке (`ERP/Mobile/Sheet Header`, `ERP/Mobile/Sheet Actions`). Сверить со скриншотом фрейма, проверить отсутствие overflow/clipping на 390px.
- [x] 20.7 Добавить `TaskCompletion`-варианты экспортов в `frontend/src/features/SalaryRuleForm/index.ts` (по уже задокументированному в файле прецеденту множественных именованных экспортов).

## 21. Frontend: `SalaryRuleForm` (`shop`) — тип правила `TaskCompletion` (TDD + UI по фрейму `ZMEof`)

Зеркало раздела 20 для направления `shop`, независимая реализация (`shop/model/*`).

- [x] 21.1 Написать тесты для `resolveShopRuleDraft()`/`draftFromShopRule()` (`shop/model/ruleFormSchema.ts`) — та же ветка `TaskCompletion` через `shopSalaryRuleRequestSchema` (задача 2.2). Verify: тесты видны раннеру.
- [x] 21.2 Прогнать тесты из 21.1, зафиксировать red.
- [x] 21.3 Реализовать: `SHOP_RULE_TYPE_ORDER`/`SHOP_RULE_TYPE_LABELS`/`SHOP_RULE_FORM_CONFIG` (`shop/model/ruleTypes.ts`), ветки `resolveShopRuleDraft`/`draftFromShopRule`.
- [x] 21.4 Прогнать тесты из 21.1, зафиксировать green, регрессий в ветках `PayPerHour`/`ProductSold`/`UsedProductSold` нет.
- [x] 21.5 Подключить `TaskCompletionRuleFields` (компонент из 20.5, переиспользуемый — поля идентичны по составу для обоих направлений, различается только `targetRole`-справочник) в форму `shop` по фрейму `ZMEof` (`Topnav` + `Content`, зеркально `service`, 1440px) — сверить со скриншотом. Если фрейм `ZMEof` показывает визуальные отличия от `wV3fv` за пределами общих полей — реализовать по фрейму, не по предположению зеркальности.

## 22. Frontend: генерическое отображение источника задачи — проверка без изменений

- [x] 22.1 Проверить (без изменения кода), что `RuleSourcesRail.tsx` (`frontend/src/pages/SalaryReportV2/ui/`) уже содержит `taskCompletion: 'Задача'` в `SOURCE_TYPE_LABELS` и рендерит `label`/`link` из `CalculationLine.sources[]` дженерик-путём — подтверждено design.md/ui-design.md, повторно проверено при подготовке tasks.md. Verify: ручной просмотр файла, без правок; при расхождении с ожиданием — завести отдельную задачу, а не молча чинить в рамках этого пункта.
- [x] 22.2 Добавить недостающую запись `taskCompletion: 'Задача'` в `SOURCE_TYPE_LABEL` (`frontend/src/features/SalaryAccruals/model/labels.ts`) — единственный реальный пробел, найденный при подготовке tasks.md: `AccrualLineSources.tsx` использует эту карту, а не `RuleSourcesRail.tsx`'ю, и в ней записи для `taskCompletion` сегодня нет (иначе показывался бы сырой код типа). Verify: `grep -n "taskCompletion" frontend/src/features/SalaryAccruals/model/labels.ts` находит новую запись.

## 23. Frontend: `TaskStatusBadge` (без Pencil-фрейма — см. примечание в начале файла)

- [x] 23.1 Реализовать `TaskStatusBadge` (`frontend/src/features/SalaryAccruals/ui/TaskStatusBadge.tsx`) — статусы «Выполнено»/«В работе»/«Просрочено» (нормализованные из `TaskStatus.code` контракта), по прецеденту `AccrualStatusBadge.tsx` (собственный `STATUS_CLASS`-record поверх той же геометрии пилюли, а не расширение `tone` у `shared/ui-kit/atoms/Badge.tsx` — этот приём в проекте уже используется для доменных статус-бейджей и не требует правки общего атома). Чисто визуальный компонент (пропс `status` → CSS-класс), тест не заводится — обоснование: нет ветвления сверх switch по 3 буквальным строкам статуса, уже валидируемым типом контракта.
- [x] 23.2 Подключить `TaskStatusBadge` в `RuleFormCard`/`TaskCompletionRuleFields` (раздел 20/21) при РЕДАКТИРОВАНИИ уже существующего правила `TaskCompletion` (когда у правила уже есть связанная задача) — рядом с заголовком/полем «Название задачи», если фрейм `wV3fv` в режиме редактирования показывает статус (сверить через `mcp__pencil__execute`/`Get` — фрейм называется «Правило-задача · Отдельная страница», не различает явно create/edit состояния в ui-design.md; если фрейм статус не показывает — оставить бейдж только в отчёте, задача 23.3, и не изобретать размещение, не подтверждённое макетом).
- [x] 23.3 Подключить `TaskStatusBadge` в `RuleSourcesRail.tsx`/строку правила `SalaryReportV2` рядом с существующим рендером `sources[]` для типа `taskCompletion` — минимальное дополнение без нового макета (ui-design.md явно относит `SalaryReportV2` к непроработанным экранам; статус здесь — точечное дополнение существующей строки, не новый экран).

## 24. Frontend: ручной ввод суммы+комментария в `SalaryAccruals` (TDD, без Pencil-фрейма)

- [x] 24.1 Написать тесты: `useSetTaskCompletionLineReward(direction, accrualId)` (`model/useAccrualMutations.ts`, по образцу `useAdjustLine`) вызывает `api.setTaskCompletionLineReward(direction, accrualId, lineId, { amount, comment })` и инвалидирует данные начисления при успехе. Verify: `npm run test -- useAccrualMutations`.
- [x] 24.2 Прогнать тест из 24.1, зафиксировать red.
  - Зафиксировано: `TypeError: useSetTaskCompletionLineReward is not a function` (хук ещё не существовал в `model/useAccrualMutations.ts`) — 3/3 теста нового файла `useAccrualMutations.spec.tsx` красные по этой причине.
- [x] 24.3 Реализовать `api.setTaskCompletionLineReward` (`model/api.ts`, `PATCH .../salary_accruals/:id/lines/:lineId/task-reward`, контракт из задачи 2.3/13.3/18.3) и хук `useSetTaskCompletionLineReward`.
- [x] 24.4 Прогнать тест из 24.1, зафиксировать green, регрессий в `useAdjustLine`/соседних мутациях нет.
  - `npx vitest run useAccrualMutations` — 3/3 зелёных; `npx vitest run src/features/SalaryAccruals` — 24/24 зелёных (без нового модального теста, добавленного пунктом 24.5 ниже).
- [x] 24.5 Написать тест: `SetTaskRewardModal` (пропсы `open`/`onOpenChange`/`line`/`direction`/`accrualId`, по образцу `AdjustLineModal.tsx`) — комментарий обязателен (кнопка сохранения задизейблена/показывает ошибку валидации при пустом комментарии, spec: «проведение отклоняется, пока комментарий не указан»), вызывает `useSetTaskCompletionLineReward` при сохранении, закрывается по успеху. Verify: тест виден раннеру.
  - Выбран вариант «показывает ошибку валидации» (не «кнопка задизейблена») — тот же приём, что уже применён в `AdjustLineModal.submit()` (валидация на клик, `setValidationError`), а не новый паттерн специально для этой модалки. Мутация замокана (`vi.mock('../model/useAccrualMutations.ts')`) — тест проверяет только собственную логику модалки (валидация/вызов мутации с нужным payload/закрытие по `onSuccess`), сама мутация уже покрыта 24.1.
- [x] 24.6 Прогнать тест из 24.5, зафиксировать red.
  - Зафиксировано: `Failed to resolve import "./SetTaskRewardModal.tsx"` (файл компонента ещё не существовал) — все 4 теста нового файла красные по этой причине.
- [x] 24.7 Реализовать `SetTaskRewardModal.tsx` (`frontend/src/features/SalaryAccruals/ui/`) — из существующих примитивов `shared/ui-kit/organisms/Modal`, `shared/ui-kit/atoms/{Button,Input,Textarea}` (без поля «Исходная сумма», в отличие от `AdjustLineModal` — design.md Decision 5: `originalAmount = 0` для этого типа строк); подключить открытие модалки для строк с `requiresManualInput === true` в `AccrualLineActions.tsx`/`AccrualLinesTable.tsx` (там, где сейчас открывается `AdjustLineModal`).
  - `AccrualLinesTable.tsx`/`AccrualLineCardList.tsx` сами не менялись — оба уже рендерят строки действий через единственный общий `AccrualLineActions.tsx` (десктоп-таблица и мобильный список карточек делят один и тот же компонент действий), поэтому веткование по `requiresManualInput` добавлено там одним местом, а не продублировано в обоих потребителях.
  - Решение сверх буквальной формулировки: для `status === 'DRAFT' && requiresManualInput === true` показывается ТОЛЬКО кнопка «Указать сумму» — кнопка «Начислить» (нулевую сумму) и карандаш «Корректировать» (`AdjustLineModal`) для такой строки скрыты, а не добавлены рядом. Обоснование в коде (`AccrualLineActions.tsx`): проведение нулевой суммы до её указания руководителем противоречит духу spec service/accounting/shop/accounting («проведение отклоняется, пока комментарий не указан»), хотя на уровне domain-сущности `accrue()`/`SetTaskCompletionLineRewardHandler` технически не блокируют такой вызов явно (backend не проверяет `requiresManualInput` в `accrue()`) — это осознанный UI-гейтинг, а не пропуск проверки бэкендом. После сохранения через `SetTaskRewardModal` бэкенд сбрасывает `requiresManualInput`, инвалидация мутации перечитывает документ, и строка на следующем рендере попадает в обычную ветку `DRAFT` (Начислить/Корректировать).
- [x] 24.8 Прогнать тест из 24.5, зафиксировать green.
  - `npx vitest run SetTaskRewardModal` — 4/4 зелёных.
- [x] 24.9 Добавить экспорт `SetTaskRewardModal`/`useSetTaskCompletionLineReward` в `frontend/src/features/SalaryAccruals/index.ts` (тот же задокументированный прецедент множественных экспортов, что у `SalaryRuleForm`).
  - Verify раздела в целом: `npx vitest run` (frontend, полный набор) — 78/78 файлов, 426/426 тестов зелёных, регрессий нет. `npx tsc -b` — новых ошибок в файлах этого раздела нет (единственная правка теста — `targetRole: 'OFFICE'` вместо невалидного `'DEPARTMENT_HEAD'`, обнаруженная тем же прогоном); оставшиеся 4 ошибки компиляции (`accrualView.spec.ts`, `ruleFormSchema.ts` ×2 service/shop) — не в изменённых этим разделом файлах (подтверждено `git diff --stat` — нет диффа), принадлежат ещё не реализованным разделам 20/21 и не связанной с этим разделом задаче в 12.4/13.4. `npx eslint <изменённые файлы>` — 0 ошибок (только уже существующий warning о legacy-синтаксисе `eslint-plugin-boundaries`, не специфичный для этих файлов).

## 25. Frontend: сквозная проверка

- [x] 25.1 Прогнать полный frontend test suite (`npm run test`) — все зелёные, регрессий нет.
  - `npx vitest run` — 78/78 файлов, 438/438 тестов зелёных (без изменений в тестах, кроме фикса 25.2 ниже).
- [x] 25.2 `npx tsc -b` и `npm run build` (frontend) без ошибок; `npx eslint .` без новых ошибок на изменённых файлах (границы FSD между `SalaryRuleForm`/`SalaryAccruals`/`SalaryReportV2` не нарушены).
  - Регрессия найдена и исправлена: `npx tsc -b` падал с `TS2719` на `src/features/SalaryAccruals/model/accrualView.spec.ts` (тестовый хелпер `line()` не задавал новые обязательные по контракту поля `comment`/`requiresManualInput` из задачи 2.3/13.3 — добавлены `comment: null, requiresManualInput: false` в дефолты хелпера). Причина ошибки была не в самом файле, а в устаревшем `contracts/dist/*` (собран `2026-09-07 22:12`, до последних правок `contracts/commands/salary-accrual.ts` от `2026-09-08 00:59`) — задача 2.4 требовала пересборки пакета, которая не была выполнена; пересобран (`cd contracts && npm run build`), после чего остался только один настоящий пробел в тестовом хелпере, устранённый правкой выше. После обеих правок — `npx tsc -b` 0 ошибок, `npm run build` (tsc -b && vite build) проходит, `npx eslint .` — 0 ошибок (только 2 pre-existing warning `[boundaries]` про legacy-синтаксис селекторов `eslint-plugin-boundaries`, не относящиеся к изменённым файлам); границы FSD не нарушены (`RuleFormCard`/`TaskCompletionRuleFields`, `SalaryAccruals`/`SetTaskRewardModal`/`TaskStatusBadge`, `RuleSourcesRail.tsx` — все импорты и барели уже проверены линтером).
- [x] 25.3 Скриншот-сверка (Pencil `mcp__pencil__` / браузер) реализованных экранов `wV3fv` (service, десктоп), `aS8yc` (service, мобильный), `ZMEof` (shop, десктоп) с фактическим рендером формы создания правила `TaskCompletion` — layout, тексты и состояния совпадают; отступления зафиксировать в примечании к этому пункту (по прецеденту раздела 20.9 change `add-bitrix24-auth-and-rbac`).
  - Сравнение сделано, но не через реальный запущенный SPA (окружение не имеет доступа к живому Bitrix24 OAuth/backend с реальными данными — `frontend/.env.local`'s `VITE_API_URL` указывает на неактивный localtunnel предыдущей сессии, приложение требует OAuth-редиректа для рендера любого защищённого экрана) — вместо этого собран временный изолированный Vite-харнесс (`RuleFormCard` — чистый presentational-компонент без своих data-хуков, принимает всё через props), отрендерен в реальном браузере (Playwright) на 1440px и 390px с драфтом типа `TaskCompletion` (`SERVICE_RULE_FORM_CONFIG`/`SHOP_RULE_FORM_CONFIG`), заскриншочен и удалён вместе со всеми временными файлами после сверки (харнесс не является частью репозитория/дерева задач).
  - `wV3fv` (service, десктоп, 1440px) — совпадает: тот же порядок полей (Название правила/Роль/Тип правила → hint «Из него формируется заголовок задачи в Bitrix24» → Описание задачи (textarea+hint) → Периодичность (3-таб SegmentedControl, 3-й таб «Сотрудники» задизейблен) + Дедлайн (date-инпут) в 2 колонки → футер «Отмена»/«Сохранить правило»). Отличие, уже осознанно задокументированное в самом коде `TaskCompletionRuleFields.tsx` (см. комментарий к компоненту) и не относящееся к этому пункту: view-only блок существующего правила (ссылка «Задача в Bitrix24», «Расчётный месяц»/«Сумма начисления») из макета `wV3fv` не реализован — вынесен в отдельную задачу по решению самого раздела 20.5.
  - `aS8yc` (service, мобильный, 390px) — совпадает: та же однокоlоночная раскладка полей без overflow/clipping, что и в макете (проверено — контент помещается по ширине 390px, скролл только вертикальный).
  - `ZMEof` (shop, десктоп, 1440px) — рендер кода совпадает с `wV3fv` 1:1 (ожидаемо: `TaskCompletionRuleFields` — один переиспользуемый компонент для обоих направлений, задача 21.5). НО обнаружено расхождение в самом `.pen`-файле: узел `ZMEof`, перечитанный через `mcp__pencil__execute`/`Get` на момент этой проверки, сейчас показывает не сценарий `TaskCompletion`, а пример «Правило 3 · Новое правило» с типом «Плавающий процент» (никаких узлов/текста про «Название задачи»/«Периодичность»/«Описание задачи»/Bitrix24 в поддереве `ZMEof` не найдено, проверено программным поиском по всему поддереву) — состояние фрейма разошлось с описанием в `ui-design.md` (зафиксировавшем `ZMEof` как «зеркально `service`») уже после его подготовки, вероятно из-за прямой правки пользователем в Pencil после того как раздел был задокументирован. Сверка «layout/тексты/состояния» для `ZMEof` в буквальном смысле пункта 25.3 сейчас невозможна — сам макет не показывает нужное состояние. Зафиксировано как openIssue, не «исправлено» молча (не в скоупе этого раздела — переключать состояние узла в `.pen`-файле не является задачей проверки).

## 26. Финальная интеграционная проверка

- [ ] 26.1 Ручной сквозной прогон на реальном/staging-портале Bitrix24 (нужен доступ к реальному вебхуку `BITRIX24_WEBHOOK_URL` — недоступен агенту без учётных данных пользователя, отметить как открытый пункт для пользователя, если прогонялось агентом): создание правила `TaskCompletion` создаёт видимую задачу в Bitrix24 с корректным ответственным/дедлайном; перевод задачи в «Выполнено» в Bitrix24 отражается в iReports в течение цикла крона (задача 8) и делает строку видимой в отчёте (FACT и PROGNOSE); ручной ввод суммы+комментария в документе начисления доступен только после этого и виден сотруднику; удаление правила закрывает задачу в Bitrix24.
  - ⚠️ Не выполнялся агентом — нет доступа к реальному/staging `BITRIX24_WEBHOOK_URL`. Также попутно выявлен нерешённый архитектурный вопрос (см. комментарии в `create-salary-rule.handler.ts`/`ensure-salary-task-for-period.service.ts` обоих направлений, разделы 11/12/16/17): для правила `TaskCompletion` на схему ОТДЕЛА `@@unique([salaryRuleId, period])` допускает только одну задачу Bitrix24 на период — ответственным становится первый сотрудник отдела, остальные сотрудники отдела видят ту же общую задачу. Это осознанно задокументированное, но не решённое дизайном ограничение — требует отдельного продуктового решения перед реальным использованием с правилами на отдел.
- [ ] 26.2 Свериться с полным списком FR/сценариев в `specs/integrations/bitrix-tasks/spec.md`, `specs/service/accounting/spec.md`, `specs/shop/accounting/spec.md` — для каждого сценария указать конкретный тест из разделов 3–24 выше, который его покрывает (таблица трассируемости в комментарии к этому пункту или в PR-описании) — не сдавать задачу, если хотя бы один сценарий спека не имеет покрывающего теста.
  - ⚠️ По собственному критерию этого пункта — НЕ закрыт: сценарий bitrix-tasks/spec.md «Изменение дедлайна в iReports обновляет задачу в Bitrix24» не имеет теста на уровне сценария (только юнит-покрытие `BitrixTasksGatewayPort.updateDeadline`/`BitrixService.updateTaskDeadline` по отдельности, без сквозного теста «поменяли дедлайн правила → вызвался updateTaskDeadline»). Сценарий «Сотрудник видит комментарий руководителя в своём начислении» покрыт только на уровне backend response DTO, не UI e2e. Остальные проверенные FR/сценарии покрытие имеют. Остаётся отдельной задачей: либо дописать недостающие тесты, либо явно принять этот пробел.
