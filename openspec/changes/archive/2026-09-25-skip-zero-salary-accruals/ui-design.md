# UI Design: skip-zero-salary-accruals

## Источник дизайна
- **.pen файл**: `design/sallary-first-iteration.pen`
- **UI Kit**: фрейм `uDEum` в этом же файле

---

## Экраны

Дизайн не требуется. Согласно `architecture.md` (`Frontend — UI Model → Pages`), единственная
затронутая страница — `SalaryReportV2` (`/salaries`, `/salaries/employee/:id`) — остаётся без
изменений: она продолжает рендерить тот же компонент строки правила теми же существующими
UI Kit-компонентами, изменяется только количество строк в приходящих с бэкенда данных (строки с
нулевой суммой правила больше не приходят). Ни новых экранов, ни новых состояний разметки, ни новых
компонентов UI Kit фича не вводит — работа в Pencil не выполнялась.

| Page (из architecture.md) | Имя фрейма в .pen | Node ID | Breakpoints (1440 / 390) | Переиспользованные компоненты UI Kit | Статус (draft/ready) |
|---|---|---|---|---|---|
| SalaryReportV2 | — (без изменений) | — | — | — | не требуется |

## Ключевые состояния экранов

_(нет — фильтрация не меняет разметку; пустая группа правил без строк — единственный пограничный
случай, и он проверяется как поведение существующего компонента на этапе реализации, см.
`design.md` → Risks, а не как новое состояние дизайна)_

## Новые компоненты UI Kit

_(нет)_

## Отклонения от architecture.md

_(нет)_

---

## Confirmation Checklist

- [x] Спроектированы все Pages из architecture.md — для единственной страницы дизайн не требуется
  (нет изменений разметки)
- [x] Новых компонентов UI Kit не создавалось
- [x] Overflow/clipping не применимо — разметка не менялась
- [ ] Пользователь подтвердил переход к tasks.md
