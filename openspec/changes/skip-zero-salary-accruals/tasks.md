## 1. Backend (service): строка начисления с нулевой суммой не создаётся — FR1

- [ ] 1.1 Написать тесты на `SalaryAccrual.createFromSnapshot` (`backend/src/domains/service/modules/accounting/domain/entities/salary-accrual/salary-accrual.entity.spec.ts`): (a) правило с рассчитанной суммой 0 не порождает `SalaryAccrualLine` в результирующем документе, (b) документ `SalaryAccrual` создаётся, даже если у сотрудника все правила дали 0 (без единой строки), (c) правило с ненулевой суммой по-прежнему создаёт строку — и убедиться, что тест-раннер их видит
- [ ] 1.2 Прогнать тесты из 1.1 и зафиксировать, что новые кейсы падают (red) по причине отсутствия фильтрации, а не из-за ошибки в тесте
- [ ] 1.3 Реализовать фильтрацию нулевых строк в `SalaryAccrual.createFromSnapshot` (не создавать `SalaryAccrualLine` для правила с `amount === 0`); обновить фикстуру/ожидания в `close-accounting-period.handler.spec.ts` (блок «документы начисления» — тест на «нулевую ставку» должен ожидать документ без строки этого правила, а не строку с суммой 0)
- [ ] 1.4 Прогнать тесты из 1.1 и весь `close-accounting-period.handler.spec.ts`/`close-accounting-period.direction-independence.spec.ts`, зафиксировать зелёный результат без регрессий

## 2. Backend (service): нулевая строка правила не отображается в зарплатном отчёте — FR2

- [x] 2.1 Написать тесты на `buildSalaryReportRules` (`backend/src/domains/service/modules/accounting/application/mappers/salary-report/to-salary-report-rules.spec.ts`): (a) строка исключается из результата, если `amount.fact === 0 && amount.prognose === 0`, (b) строка остаётся, если хотя бы одно из значений отлично от нуля, (c) строка правила «за выполнение задачи» не исключается общим фильтром, даже когда `fact === 0 && prognose === 0` (например, сумма правила по умолчанию настроена в 0, но задача уже заведена) — и убедиться, что тест-раннер их видит
- [x] 2.2 Прогнать тесты из 2.1 и зафиксировать, что новые кейсы падают (red) по причине отсутствия фильтрации
- [x] 2.3 Реализовать фильтрацию нулевых строк в `buildSalaryReportRules` (исключение по типу для правила «за выполнение задачи»)
- [x] 2.4 Прогнать тесты из 2.1 и e2e-тесты зарплатного отчёта сервиса, зафиксировать зелёный результат без регрессий

## 3. Backend (shop): строка начисления с нулевой суммой не создаётся — FR3

- [x] 3.1 Написать тесты на `SalaryAccrual.createFromSnapshot` домена shop (`backend/src/domains/shop/modules/accounting/domain/entities/salary-accrual/salary-accrual.entity.spec.ts` — завести файл, если отсутствует) с теми же тремя кейсами, что в 1.1, и убедиться, что тест-раннер их видит
- [x] 3.2 Прогнать тесты из 3.1 и зафиксировать red по причине отсутствия фильтрации
- [x] 3.3 Реализовать ту же фильтрацию нулевых строк в shop-реализации `SalaryAccrual.createFromSnapshot`; обновить фикстуры в `close-accounting-period.handler.spec.ts` домена shop под новое поведение
- [x] 3.4 Прогнать тесты из 3.1 и весь набор spec для `close-accounting-period` домена shop, зафиксировать зелёный результат без регрессий

## 4. Backend (shop): нулевая строка правила не отображается в зарплатном отчёте — FR4

- [ ] 4.1 Написать тесты на `buildSalaryReportRules` (`backend/src/domains/shop/modules/accounting/application/mappers/salary-report/to-salary-report-rules.spec.ts`) с теми же тремя кейсами, что в 2.1, и убедиться, что тест-раннер их видит
- [ ] 4.2 Прогнать тесты из 4.1 и зафиксировать red по причине отсутствия фильтрации
- [ ] 4.3 Реализовать ту же фильтрацию нулевых строк в shop-реализации `buildSalaryReportRules`
- [ ] 4.4 Прогнать тесты из 4.1 и `get-employee-salary-report.service.spec.ts`/`get-department-salary-report.service.spec.ts`/`get-employee-salary-report.e2e.spec.ts` домена shop, зафиксировать зелёный результат без регрессий

## 5. Frontend: проверка отсутствия визуальной регрессии

- [ ] 5.1 Запустить фронтенд (`pages/SalaryReportV2`, `/salaries/employee/:id`) на периоде с сотрудником, у которого хотя бы одно правило даёт 0, и вручную убедиться, что: строка с нулевым правилом не отображается, группа правила (если это единственное правило категории) не рендерит пустую панель, а строки с ненулевыми значениями отображаются как раньше. Тесты не заводятся — чисто визуальная проверка существующей вёрстки без изменения кода фронтенда (см. design.md → Risks)

## 6. Полная регрессия

- [ ] 6.1 Прогнать полный backend test suite (`npm test` / `nx test backend` — команда по `backend/CLAUDE.md`) и зафиксировать отсутствие непредвиденных падений вне затронутых файлов
