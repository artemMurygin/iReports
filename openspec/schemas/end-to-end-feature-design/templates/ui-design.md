<!--
  Artifact: ui-design
  Requires: proposal, design, architecture
  Required by: tasks (tasks.md не должен создаваться, пока экраны не согласованы с пользователем)

  Инструкция для агента:

  - Дизайн создаётся в Pencil, в общем файле проекта design/sallary-first-iteration.pen —
    это единственный источник дизайна iReports (все экраны всех фич живут в одном
    .pen файле). НЕ создавай новый .pen файл, если пользователь явно не попросил
    отдельный документ.
  - .pen файлы зашифрованы — работай с ними ТОЛЬКО через mcp__pencil__* MCP-инструменты
    (execute / get_app_state / get_style / read_skill), никогда через Read/Grep/Edit/Write.

  Перед началом:
  1. Если mcp__pencil__* ещё не загружены — один ToolSearch:
     "select:mcp__pencil__get_app_state,mcp__pencil__execute,mcp__pencil__get_style,mcp__pencil__read_skill".
  2. Загрузи скилл `pencil-design` (design-to-code практики проекта) — он требует
     сначала загрузить `frontend-design` (эстетическое направление), чтобы не
     получить типовой AI-дизайн.
  3. `mcp__pencil__read_skill()` и нужные guide/*.md (design-system, web-app,
     mobile-app, table и т.д.) — актуальный API инструмента `execute` для этого
     проекта; не полагайся на память из предыдущих задач, API мог обновиться.
  4. `get_app_state` по design/sallary-first-iteration.pen, затем через
     `execute`/`Get` прочитай UI Kit фрейм (node id `uDEum`) — это единственный
     источник переиспользуемых компонентов проекта (кнопки, инпуты, карточки,
     KPI-карточки, таблицы, чипы, бейджи и т.д.).

  Что и как проектировать:
  - Ровно те Pages, что перечислены в architecture.md ("Frontend — UI Model →
    Pages"), и их ключевые состояния (пусто/загрузка/ошибка/закрытый период и
    т.п.), если состояние меняет разметку, а не только текст. Не придумывай
    экраны "на всякий случай" сверх того, что есть в architecture.md — если
    во время дизайна выяснилось, что нужен ещё один экран или состояние,
    сначала согласуй с пользователем.
  - Везде, где в UI Kit (`uDEum`) есть подходящий компонент — вставляй его
    как `ref`-инстанс и кастомизируй через `descendants`/`Update`. Создавай новый
    компонент внутри `uDEum` (не рядом с экраном) только если подходящего
    действительно нет, и зафиксируй это в разделе "Новые компоненты" ниже.
  - Для каждой страницы — минимум десктоп-артборд (1440). Мобильный артборд
    (390) добавляй, если фича должна быть доступна с телефона — см.
    proposal.md/architecture.md, уточни у пользователя, если неочевидно.
  - Новые экраны — top-level фреймы в `document`, ниже уже существующих макетов
    (используй `FindEmptySpace`, чтобы не перекрыть существующее), с
    `clip: true` и `placeholder: true` на время работы над фреймом.
  - Используй переменные ($-токены) файла для цвета/радиуса/отступов/шрифтов —
    не хардкодь значения, которые уже есть в `GetVariables()`.

  Проверка:
  - Работай по одному разделу экрана за раз (шапка, фильтры, таблица, карточка,
    состояние и т.д.): вставил — `TakeScreenshot` — проверил layout (`Get` с
    визитором на clipping/overflow, контраст, выравнивание) — исправил — пошёл
    дальше. Не строй весь экран целиком без промежуточных проверок.
  - Финальный скриншот делай на каждый готовый экран целиком.

  По каждому законченному экрану заполняй таблицу "Экраны" ниже — Node ID нужен
  исполнителям в tasks.md, чтобы затем читать точную структуру макета через
  `execute`/`Get` (пример такого использования — .claude/workflows/salary-report-frontend.js,
  где агентам передавались конкретные node id фреймов из этого же .pen файла).

  ОБЯЗАТЕЛЬНО: после заполнения всех разделов останови работу и явно спроси
  пользователя подтверждение по разделу "Confirmation Checklist" ниже. Не
  переходи к tasks.md без явного "ок"/"согласен" от пользователя.
-->

# UI Design: {change-name}

## Источник дизайна
- **.pen файл**: `design/sallary-first-iteration.pen`
- **UI Kit**: фрейм `uDEum` в этом же файле — переиспользуемые компоненты (кнопки, инпуты, карточки, KPI, таблицы, чипы, бейджи и т.д.)

---

## Экраны
<!-- По одной строке на каждую Page из architecture.md -->
| Page (из architecture.md) | Имя фрейма в .pen | Node ID | Breakpoints (1440 / 390) | Переиспользованные компоненты UI Kit | Статус (draft/ready) |
|---|---|---|---|---|---|
| | | | | | |

## Ключевые состояния экранов
<!-- Только состояния, реально влияющие на разметку (пусто/загрузка/ошибка/закрытый период и т.п.) -->
| Экран | Состояние | Как показано (отдельный артборд / вариант компонента) | Node ID (если отдельный артборд) |
|---|---|---|---|
| | | | |

## Новые компоненты UI Kit
<!-- Заполняй, только если в uDEum не нашлось подходящего компонента. Каждый такой компонент
     должен быть сохранён как reusable ВНУТРИ фрейма uDEum, а не рядом с экраном -->
| Компонент | Node ID (внутри uDEum) | Где используется | Почему существующего компонента недостаточно |
|---|---|---|---|
| | | | |

## Отклонения от architecture.md
<!-- Если в процессе дизайна пришлось скорректировать состав полей/сущностей из architecture.md
     (например, поле не помещается в макет или экрану нужно доп. состояние) — опиши здесь
     и вернись согласовать с пользователем. Пусто, если отклонений нет -->

---

## Confirmation Checklist
<!-- Агент: выведи этот список пользователю явным текстом перед завершением артефакта -->
- [ ] Спроектированы все Pages из architecture.md (десктоп + мобильный там, где он нужен)
- [ ] Везде, где был подходящий компонент UI Kit — использован `ref` на `uDEum`, а не пересоздан с нуля
- [ ] Новые компоненты (если есть) сохранены как reusable внутри `uDEum`
- [ ] Нет overflow/clipping (layout проверен на каждом экране)
- [ ] Пользователь посмотрел экраны (в Pencil или по скриншотам) и подтвердил дизайн
- [ ] Пользователь подтвердил переход к tasks.md
