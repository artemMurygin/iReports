# Временно отключено: backend ещё не проходит lint/test после рефакторинга.
# Чтобы включить обратно, раскомментируй блок ниже.
exit 0

# echo "▶ backend: lint"
# npm run backend:lint || exit 1
#
# echo "▶ backend: test"
# npm run backend:test || exit 1
#
# echo "▶ frontend: lint"
# npm run frontend:lint || exit 1