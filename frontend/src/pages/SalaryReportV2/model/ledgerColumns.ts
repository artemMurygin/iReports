/**
 * Общие ширины колонок «Факт, ₽» / «Прогноз, ₽» и хвостового шеврона внутри карточки-гроссбуха
 * (Pencil: `H7Mz74` "Ledger · Зарплата" — 140px десктоп / `b63e8p`'s `UcVqU` — 80px мобайл) — один
 * набор классов, которым пользуются заголовок колонок, строка правила и её "Rail"-детализация
 * (`LedgerDirectionBlock`/`LedgerRuleRow`/`RuleSourcesRail`), чтобы все три уровня оставались
 * пиксель-в-пиксель выровненными по вертикали без ручного дублирования числа в каждом файле.
 */
export const LEDGER_VALUE_COL = 'w-20 shrink-0 text-right md:w-[140px]'

/** Хвостовая колонка шеврона разворота строки — в макете отдельного узла под неё нет (мокап
 * показывает уже развёрнутую строку без явного шеврона), но контракт пропсов требует рабочего
 * `onToggleRule`, поэтому аффорданс добавлен как узкий четвёртый "столбец" сверх макета. */
export const LEDGER_CHEVRON_COL = 'flex w-4 shrink-0 justify-end md:w-5'
